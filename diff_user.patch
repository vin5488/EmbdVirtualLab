diff --git a/index.html b/index_user.html
index 63c3238c..fbfbf5fe 100644
--- a/index.html
+++ b/index_user.html
@@ -1,4 +1,4 @@
-<!DOCTYPE html>
+∩╗┐<!DOCTYPE html>
 <html lang="en">
 
 <head>
@@ -15,82 +15,195 @@
     <script src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js"></script>
 
     <!-- CodeMirror 6 for Syntax Highlighting -->
+    <script type="importmap">
+    {
+      "imports": {
+        "codemirror": "https://esm.sh/codemirror@6.0.1",
+        "@codemirror/view": "https://esm.sh/@codemirror/view@6",
+        "@codemirror/state": "https://esm.sh/@codemirror/state@6",
+        "@codemirror/language": "https://esm.sh/@codemirror/language@6",
+        "@codemirror/commands": "https://esm.sh/@codemirror/commands@6",
+        "@codemirror/lang-cpp": "https://esm.sh/@codemirror/lang-cpp@6",
+        "@codemirror/theme-one-dark": "https://esm.sh/@codemirror/theme-one-dark@6",
+        "@codemirror/autocomplete": "https://esm.sh/@codemirror/autocomplete@6",
+        "@codemirror/lint": "https://esm.sh/@codemirror/lint@6",
+        "@codemirror/search": "https://esm.sh/@codemirror/search@6",
+        "@lezer/common": "https://esm.sh/@lezer/common@1",
+        "@lezer/highlight": "https://esm.sh/@lezer/highlight@1",
+        "@lezer/lr": "https://esm.sh/@lezer/lr@1",
+        "@lezer/cpp": "https://esm.sh/@lezer/cpp@1"
+      }
+    }
+    </script>
     <script type="module">
-        import { EditorView, basicSetup } from "https://cdn.jsdelivr.net/npm/codemirror@6.0.1/dist/index.js"
-        import { cpp } from "https://cdn.jsdelivr.net/npm/@codemirror/lang-cpp@6.0.2/dist/index.js"
-        import { oneDark } from "https://cdn.jsdelivr.net/npm/@codemirror/theme-one-dark@6.1.2/dist/index.js"
+        import { EditorView, basicSetup } from "codemirror"
+        import { cpp } from "@codemirror/lang-cpp"
+        import { oneDark } from "@codemirror/theme-one-dark"
         window.CodeMirrorModules = { EditorView, basicSetup, cpp, oneDark };
     </script>
     <style>
         @import url('https://fonts.googleapis.com/css2?family=Fira+Code:wght@400;500&family=Inter:wght@300;400;600;700&display=swap');
-        body { font-family: 'Inter', sans-serif; }
-        .code-font { font-family: 'Fira Code', monospace; }
-        [x-cloak] { display: none !important; }
-        ::-webkit-scrollbar { width: 6px; height: 6px; }
-        ::-webkit-scrollbar-track { background: #0f172a; }
-        ::-webkit-scrollbar-thumb { background: #334155; border-radius: 10px; }
-        .custom-scrollbar::-webkit-scrollbar { width: 4px; }
-        .wiring-wire { transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1); }
-        .line-numbers { overflow-y: hidden; overflow-x: hidden; min-width: 40px; }
-        .line-numbers div { user-select: none; }
-        #code-editor { height: 100%; font-size: 14px; }
-        #code-editor .cm-editor { height: 100%; background: transparent !important; }
-        #code-editor .cm-scroller { font-family: 'Fira Code', monospace; line-height: 1.6; }
-        #code-editor .cm-gutters { background: rgba(15,23,42,0.5) !important; border-right: 1px solid #1e293b !important; }
-
-        /* ΓöÇΓöÇ Problem Statement Animations ΓöÇΓöÇ */
-        @keyframes ps-fade-up { from { opacity:0; transform:translateY(18px); } to { opacity:1; transform:translateY(0); } }
-        @keyframes ps-fade-in { from { opacity:0; } to { opacity:1; } }
-        @keyframes ps-slide-right { from { opacity:0; transform:translateX(-16px); } to { opacity:1; transform:translateX(0); } }
-        @keyframes ps-pop { 0%{opacity:0;transform:scale(0.88);} 60%{transform:scale(1.04);} 100%{opacity:1;transform:scale(1);} }
-        @keyframes badge-glow { 0%,100%{box-shadow:0 0 0 0 rgba(59,130,246,0);} 50%{box-shadow:0 0 14px 4px rgba(59,130,246,0.4);} }
-        @keyframes crit-check { 0%{opacity:0;transform:scale(0) rotate(-30deg);} 70%{transform:scale(1.2) rotate(5deg);} 100%{opacity:1;transform:scale(1) rotate(0);} }
-        @keyframes shimmer { 0%{background-position:-400px 0;} 100%{background-position:400px 0;} }
-        @keyframes pulse-dot { 0%,100%{opacity:1;transform:scale(1);} 50%{opacity:0.45;transform:scale(1.5);} }
-        @keyframes title-reveal { 0%{opacity:0;letter-spacing:0.25em;} 100%{opacity:1;letter-spacing:normal;} }
-        @keyframes underline-grow { from{width:0;} to{width:100%;} }
-        @keyframes float-chip { 0%,100%{transform:translateY(0px);} 50%{transform:translateY(-5px);} }
-        @keyframes count-up { from{opacity:0;transform:translateY(6px);} to{opacity:1;transform:translateY(0);} }
-
-        .ps-header   { animation: ps-fade-in 0.4s ease both; }
-        .ps-title    { animation: title-reveal 0.55s ease both 0.05s; }
-        .ps-badge-id   { animation: ps-pop 0.4s ease both 0.08s; }
-        .ps-badge-diff { animation: ps-pop 0.4s ease both 0.16s; }
-        .ps-points-badge { animation: ps-pop 0.4s ease both 0.24s; }
-        .ps-statement  { animation: ps-fade-up 0.5s ease both 0.15s; }
-        .ps-criteria   { animation: ps-fade-up 0.5s ease both 0.25s; }
-        .ps-sidebar    { animation: ps-slide-right 0.5s ease both 0.3s; }
-        .ps-inout-card { animation: ps-fade-up 0.45s ease both 0.35s; }
-        .ps-tips       { animation: ps-fade-up 0.5s ease both 0.42s; }
-
-        .ps-crit-item { opacity:0; animation: ps-fade-up 0.35s ease forwards; }
-        .ps-crit-icon { animation: crit-check 0.4s ease both; }
-        .ps-point-badge { animation: badge-glow 2.2s ease-in-out 1.2s infinite; }
-        .ps-live-dot { animation: pulse-dot 1.5s ease-in-out infinite; }
-        .ps-float-icon { animation: float-chip 3s ease-in-out infinite; }
-
-        .ps-shimmer-card { position:relative; overflow:hidden; }
-        .ps-shimmer-card::after {
-            content:''; position:absolute; top:0; left:-100%; width:60%; height:100%;
-            background:linear-gradient(90deg,transparent,rgba(255,255,255,0.04),transparent);
-            animation: shimmer 2.2s ease 0.6s 1;
+
+        body {
+            font-family: 'Inter', sans-serif;
+        }
+
+        /* ΓöÇΓöÇ Block text selection for candidates ΓöÇΓöÇ */
+        body.candidate-mode *:not(textarea):not(input):not(pre):not(code) {
+            -webkit-user-select: none;
+            -moz-user-select: none;
+            user-select: none;
+        }
+        body.candidate-mode textarea,
+        body.candidate-mode .cm-editor {
+            -webkit-user-select: text;
+            user-select: text;
+        }
+
+
+        .ticker-track {
+            display: inline-flex;
+            animation: ticker-scroll 60s linear infinite;
+        }
+        .ticker-track:hover {
+            animation-play-state: paused;
+        }
+        @keyframes ticker-scroll {
+            0%   { transform: translateX(0); }
+            100% { transform: translateX(-50%); }
+        }
+
+        .code-font {
+            font-family: 'Fira Code', monospace;
+        }
+
+        [x-cloak] {
+            display: none !important;
+        }
+
+        ::-webkit-scrollbar {
+            width: 6px;
+            height: 6px;
+        }
+
+        ::-webkit-scrollbar-track {
+            background: #0f172a;
+        }
+
+        ::-webkit-scrollbar-thumb {
+            background: #334155;
+            border-radius: 10px;
+        }
+
+        .custom-scrollbar::-webkit-scrollbar {
+            width: 4px;
+        }
+
+        .wiring-wire {
+            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
+        }
+
+        /* Line numbers styling */
+        .line-numbers {
+            overflow-y: hidden;
+            overflow-x: hidden;
+            min-width: 40px;
+        }
+
+        .line-numbers div {
+            user-select: none;
+        }
+
+        /* CodeMirror styling */
+        #code-editor {
+            height: 100%;
+            font-size: 14px;
+        }
+
+        #code-editor .cm-editor {
+            height: 100%;
+            background: transparent !important;
+        }
+
+        #code-editor .cm-scroller {
+            font-family: 'Fira Code', monospace;
+            line-height: 1.6;
         }
-        .ps-title-underline {
-            display:block; height:2px; background:linear-gradient(90deg,#3b82f6,#8b5cf6,transparent);
-            border-radius:2px; margin-top:6px;
-            animation: underline-grow 0.6s ease both 0.5s;
-            width:0;
+
+        #code-editor .cm-gutters {
+            background: rgba(15, 23, 42, 0.5) !important;
+            border-right: 1px solid #1e293b !important;
         }
-        .ps-difficulty-fill { width:0; transition: width 0.9s cubic-bezier(0.34,1.56,0.64,1) 0.7s; }
-        .ps-tag { animation: ps-pop 0.35s ease both; }
-        .ps-count { animation: count-up 0.4s ease both 0.6s; }
     </style>
+    <script>
+        // ΓöÇΓöÇ Visteon-only access enforcement ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
+        window.isVisteonUser = function(email) {
+            return typeof email === 'string' && email.toLowerCase().endsWith('@visteon.com');
+        };
+        // Admin/staff accounts (skill-lync.com) bypass the visteon-only restriction
+        window.isAllowedUser = function(email) {
+            if (typeof email !== 'string') return false;
+            const e = email.toLowerCase();
+            return e.endsWith('@visteon.com') || e.endsWith('@skill-lync.com');
+        };
+    </script>
 </head>
 
-<body class="bg-slate-950 text-slate-200 h-screen overflow-hidden" x-data="labApp()">
+<body class="bg-slate-950 text-slate-200 h-screen overflow-hidden" x-data="labApp()"
+    :class="{ 'candidate-mode': isLoggedIn && user.role === 'candidate' }">
 
     <!-- 1. Authentication Gateway -->
-    <div x-show="!isLoggedIn" class="fixed inset-0 z-[100] bg-slate-950 flex items-center justify-center p-4">
+    <div x-show="!isLoggedIn" class="fixed inset-0 z-[100] bg-slate-950 flex flex-col items-stretch justify-start">
+
+        <!-- ΓöÇΓöÇ How-To Ticker Ribbon ΓöÇΓöÇ -->
+        <div class="w-full bg-gradient-to-r from-blue-900/80 via-slate-900/90 to-blue-900/80 border-b border-blue-700/40 py-2.5 overflow-hidden flex items-center gap-3 shrink-0">
+            <div class="shrink-0 flex items-center gap-2 px-4 border-r border-blue-700/40">
+                <i class="fas fa-info-circle text-blue-400 text-xs animate-pulse"></i>
+                <span class="text-[10px] font-black text-blue-400 uppercase tracking-widest whitespace-nowrap">How It Works</span>
+            </div>
+            <div class="overflow-hidden flex-1 relative">
+                <div class="ticker-track flex gap-16 items-center whitespace-nowrap">
+                    <template x-for="item in [
+                        { icon: 'fa-envelope', color: 'text-blue-400',  text: 'Step 1 ΓÇö Enter your Visteon email and click Generate Access Code' },
+                        { icon: 'fa-key',      color: 'text-amber-400', text: 'Step 2 ΓÇö Check your inbox for a one-time OTP and enter it here' },
+                        { icon: 'fa-book-open',color: 'text-emerald-400',text: 'Step 3 ΓÇö Select a topic from the dashboard to view your assignment problems' },
+                        { icon: 'fa-code',     color: 'text-purple-400', text: 'Step 4 ΓÇö Open a problem, read the statement, and write your C code in the editor' },
+                        { icon: 'fa-clock',    color: 'text-orange-400', text: 'Step 5 ΓÇö Each problem has a 15-minute timer. Finish early to bank time for later problems' },
+                        { icon: 'fa-play-circle', color: 'text-teal-400', text: 'Step 6 ΓÇö Click Compile & Run Tests to validate your solution against test cases' },
+                        { icon: 'fa-paper-plane', color: 'text-pink-400', text: 'Step 7 ΓÇö Once tests pass, click Submit Assignment to record your submission' },
+                        { icon: 'fa-shield-alt', color: 'text-red-400',  text: 'ΓÜá Do NOT copy-paste or switch tabs ΓÇö all activity is monitored and logged' },
+                        { icon: 'fa-check-double', color: 'text-emerald-400', text: 'Tip ΓÇö You can move to the next problem anytime; unused time carries forward' }
+                    ]" :key="item.text">
+                        <span class="inline-flex items-center gap-2 text-[11px] text-slate-300 font-medium">
+                            <i class="fas text-xs shrink-0" :class="[item.icon, item.color]"></i>
+                            <span x-text="item.text"></span>
+                        </span>
+                        <span class="text-blue-700/60 text-xs shrink-0">Γùå</span>
+                    </template>
+                    <!-- duplicate for seamless loop -->
+                    <template x-for="item in [
+                        { icon: 'fa-envelope', color: 'text-blue-400',  text: 'Step 1 ΓÇö Enter your Visteon email and click Generate Access Code' },
+                        { icon: 'fa-key',      color: 'text-amber-400', text: 'Step 2 ΓÇö Check your inbox for a one-time OTP and enter it here' },
+                        { icon: 'fa-book-open',color: 'text-emerald-400',text: 'Step 3 ΓÇö Select a topic from the dashboard to view your assignment problems' },
+                        { icon: 'fa-code',     color: 'text-purple-400', text: 'Step 4 ΓÇö Open a problem, read the statement, and write your C code in the editor' },
+                        { icon: 'fa-clock',    color: 'text-orange-400', text: 'Step 5 ΓÇö Each problem has a 15-minute timer. Finish early to bank time for later problems' },
+                        { icon: 'fa-play-circle', color: 'text-teal-400', text: 'Step 6 ΓÇö Click Compile & Run Tests to validate your solution against test cases' },
+                        { icon: 'fa-paper-plane', color: 'text-pink-400', text: 'Step 7 ΓÇö Once tests pass, click Submit Assignment to record your submission' },
+                        { icon: 'fa-shield-alt', color: 'text-red-400',  text: 'ΓÜá Do NOT copy-paste or switch tabs ΓÇö all activity is monitored and logged' },
+                        { icon: 'fa-check-double', color: 'text-emerald-400', text: 'Tip ΓÇö You can move to the next problem anytime; unused time carries forward' }
+                    ]" :key="'dup-' + item.text">
+                        <span class="inline-flex items-center gap-2 text-[11px] text-slate-300 font-medium">
+                            <i class="fas text-xs shrink-0" :class="[item.icon, item.color]"></i>
+                            <span x-text="item.text"></span>
+                        </span>
+                        <span class="text-blue-700/60 text-xs shrink-0">Γùå</span>
+                    </template>
+                </div>
+            </div>
+        </div>
+
+        <!-- login content centred in remaining space -->
+        <div class="flex-1 flex items-center justify-center p-4 overflow-y-auto">
         <div class="max-w-md w-full">
             <div class="text-center mb-10">
                 <div class="inline-flex items-center gap-3 mb-4">
@@ -188,9 +301,8 @@
                 </div>
             </template>
         </div>
+        </div>
     </div>
-
-    <!-- 2. Main Lab Interface -->
     <div x-show="isLoggedIn" class="h-screen flex flex-col" x-cloak>
         <!-- Header -->
         <header
@@ -354,7 +466,7 @@
                         <div>
                             <div class="text-[10px] font-black text-slate-500 uppercase tracking-widest">Solved</div>
                             <div class="text-2xl font-black text-white"
-                                x-text="solvedCount + ' / ' + (selectedTopic?.id === 'VISTEON_C' ? 100 : (allowedProjects.length || projects.length))">
+                                x-text="solvedCount + ' / ' + (allowedProjects.length || projects.length)">
                             </div>
                         </div>
                     </div>
@@ -409,7 +521,7 @@
                             <p class="text-xs text-slate-500 mb-6 leading-relaxed" x-text="topic.desc"></p>
                             <div class="flex items-center justify-between mt-auto pt-4 border-t border-slate-800">
                                 <span class="text-[10px] font-bold text-blue-500 uppercase tracking-widest"
-                                    x-text="topic.projectCount + ' Projects'"></span>
+                                    x-text="projects.filter(p => p.topicId === topic.id).length + ' Problems'"></span>
                                 <i
                                     class="fas fa-arrow-right text-slate-600 group-hover:text-blue-500 transition-all"></i>
                             </div>
@@ -449,11 +561,24 @@
                                     <i x-show="project.solved"
                                         class="fas fa-check-circle text-emerald-500 text-[10px]"></i>
                                 </div>
-                                <div class="flex items-center gap-4">
+                                <div class="flex items-center gap-3 flex-wrap">
                                     <span class="text-[9px] text-slate-600 font-bold uppercase"
                                         x-text="project.difficulty"></span>
                                     <span class="text-[9px] text-slate-600 font-bold"><i class="fas fa-award mr-1"></i>
                                         <span x-text="project.points"></span></span>
+                                    <!-- Timer indicator for active problem -->
+                                    <span x-show="activeProject.id === project.id && problemStartTime && user.role === 'candidate'"
+                                        class="inline-flex items-center gap-1 text-[9px] font-black px-1.5 py-0.5 rounded-md"
+                                        :class="timerRemainingSeconds <= 60 ? 'bg-red-900/50 text-red-400' : timerRemainingSeconds <= 180 ? 'bg-amber-900/50 text-amber-400' : 'bg-slate-700 text-slate-400'">
+                                        <i class="fas fa-clock"></i>
+                                        <span x-text="timerDisplay"></span>
+                                    </span>
+                                    <!-- Violations badge for this problem -->
+                                    <span x-show="user.role === 'candidate' && violationLog.filter(v => v.problem === project.title).length > 0"
+                                        class="inline-flex items-center gap-1 text-[9px] font-black bg-red-900/50 text-red-400 px-1.5 py-0.5 rounded-md">
+                                        <i class="fas fa-exclamation-triangle"></i>
+                                        <span x-text="violationLog.filter(v => v.problem === project.title).length + ' violation' + (violationLog.filter(v => v.problem === project.title).length > 1 ? 's' : '')"></span>
+                                    </span>
                                 </div>
                             </div>
                         </template>
@@ -488,9 +613,25 @@
                         <!-- Spacer to push buttons to right -->
                         <div class="flex-1"></div>
 
+                        <!-- Problem Timer (candidates only) -->
+                        <div x-show="isLoggedIn && user.role === 'candidate' && problemStartTime"
+                            class="flex items-center gap-3 px-4 py-1.5 rounded-xl border"
+                            :class="timerRemainingSeconds <= 60 ? 'bg-red-950/50 border-red-700/50' : timerRemainingSeconds <= 180 ? 'bg-amber-950/50 border-amber-700/50' : 'bg-slate-900 border-slate-700'">
+                            <i class="fas fa-clock text-xs" :class="timerColor"></i>
+                            <div class="text-xs font-black font-mono" :class="timerColor" x-text="timerDisplay"></div>
+                            <div class="text-[9px] text-slate-500 uppercase tracking-widest">remaining</div>
+                        </div>
+
                         <!-- Action Buttons -->
                         <div class="flex items-center gap-2 pr-4">
-                            <button x-show="buildReport" @click="downloadLogs"
+                            <!-- Auto-save indicator -->
+                            <div x-show="autoSaveStatus && user.role === 'candidate'"
+                                class="flex items-center gap-1.5 text-[9px] font-bold px-2 py-1 rounded-lg"
+                                :class="autoSaveStatus === 'saved' ? 'text-emerald-400 bg-emerald-950/50' : 'text-slate-500'">
+                                <i class="fas" :class="autoSaveStatus === 'saved' ? 'fa-check-circle' : 'fa-circle-notch fa-spin'"></i>
+                                <span x-text="autoSaveStatus === 'saved' ? 'Auto-saved' : 'Saving...'"></span>
+                            </div>
+                            <button x-show="buildReport && user.role === 'admin'" @click="downloadLogs"
                                 class="bg-slate-800 hover:bg-slate-700 text-slate-400 hover:text-white px-3 py-1.5 rounded-lg font-bold text-[10px] transition-all flex items-center gap-2 border border-slate-700">
                                 <i class="fas fa-download"></i> Save Report
                             </button>
@@ -500,7 +641,7 @@
                                 <span x-text="isBuilding ? 'Building...' : 'Compile & Run Tests'"></span>
                             </button>
                             <button x-show="buildReport && buildReport.success && testResults.length > 0"
-                                @click="showSubmitModal = true"
+                                @click="checkOffDayThenSubmit()"
                                 class="bg-emerald-600 hover:bg-emerald-500 text-white px-4 py-1.5 rounded-lg font-bold text-[10px] transition-all flex items-center gap-2 shadow-lg shadow-emerald-900/20">
                                 <i class="fas fa-paper-plane"></i> Submit Assignment
                             </button>
@@ -511,174 +652,77 @@
 
                         <!-- Problem View -->
                         <div x-show="wsTab === 'problem'"
-                            class="absolute inset-0 overflow-y-auto custom-scrollbar"
-                            style="background: linear-gradient(135deg, #020817 0%, #0a1628 50%, #020817 100%);">
-
-                            <!-- Animated top accent bar -->
-                            <div style="height:3px; background:linear-gradient(90deg,#3b82f6,#8b5cf6,#06b6d4); animation:underline-grow 0.8s ease both;"></div>
-
-                            <div class="p-8 max-w-6xl mx-auto">
-
-                                <!-- Header row -->
-                                <div class="ps-header flex items-start justify-between mb-8 gap-4">
-                                    <div class="flex-1 min-w-0">
-                                        <!-- Floating chip icon -->
-                                        <div class="flex items-center gap-3 mb-3">
-                                            <div class="ps-float-icon w-9 h-9 rounded-xl flex items-center justify-center shrink-0"
-                                                style="background:linear-gradient(135deg,#1d4ed8,#7c3aed); box-shadow:0 0 18px rgba(99,102,241,0.4);">
-                                                <i class="fas fa-microchip text-white text-sm"></i>
-                                            </div>
-                                            <span class="text-xs font-bold uppercase tracking-widest text-slate-500">Problem Statement</span>
-                                            <span class="ps-live-dot w-2 h-2 rounded-full bg-emerald-400 inline-block"></span>
-                                        </div>
-                                        <!-- Title with underline animation -->
-                                        <div>
-                                            <h2 class="ps-title text-3xl font-black text-white leading-tight m-0" x-text="activeProject.title"></h2>
-                                            <span class="ps-title-underline"></span>
-                                        </div>
-                                    </div>
-                                    <!-- Badges -->
-                                    <div class="flex flex-col gap-2 items-end shrink-0 mt-1">
-                                        <span class="ps-badge-id px-3 py-1 rounded-full text-[10px] font-black border"
-                                            style="background:rgba(59,130,246,0.12);color:#60a5fa;border-color:rgba(59,130,246,0.3);"
-                                            x-text="activeProject.id"></span>
-                                        <span class="ps-badge-diff px-3 py-1 rounded-full text-[10px] font-black uppercase"
-                                            style="background:rgba(100,116,139,0.2);color:#94a3b8;"
-                                            x-text="activeProject.difficulty"></span>
-                                        <span class="ps-points-badge ps-point-badge px-3 py-1 rounded-full text-[10px] font-black flex items-center gap-1"
-                                            style="background:rgba(234,179,8,0.12);color:#facc15;border:1px solid rgba(234,179,8,0.3);">
-                                            <i class="fas fa-star text-[9px]"></i>
-                                            <span x-text="activeProject.points"></span> pts
-                                        </span>
-                                    </div>
+                            class="absolute inset-0 overflow-y-auto p-10 prose prose-invert max-w-none">
+                            <div class="flex items-center justify-between mb-8">
+                                <h2 class="text-3xl font-black text-white m-0" x-text="activeProject.title"></h2>
+                                <div class="flex gap-2">
+                                    <span
+                                        class="bg-blue-600/20 text-blue-400 px-3 py-1 rounded-full text-[10px] font-bold border border-blue-600/30"
+                                        x-text="activeProject.id"></span>
+                                    <span
+                                        class="bg-slate-800 text-slate-400 px-3 py-1 rounded-full text-[10px] font-bold uppercase"
+                                        x-text="activeProject.difficulty"></span>
                                 </div>
+                            </div>
 
-                                <!-- Main 2-col grid -->
-                                <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
-
-                                    <!-- Left: Statement + Criteria -->
-                                    <div class="lg:col-span-2 space-y-5">
-
-                                        <!-- Statement card -->
-                                        <div class="ps-statement ps-shimmer-card rounded-2xl border p-7"
-                                            style="background:rgba(15,23,42,0.8);border-color:rgba(59,130,246,0.2);">
-                                            <div class="flex items-center gap-2 mb-4">
-                                                <div class="w-1 h-5 rounded-full" style="background:linear-gradient(180deg,#3b82f6,#8b5cf6);"></div>
-                                                <h4 class="text-blue-400 text-xs font-black uppercase tracking-widest m-0">Detailed Problem Statement</h4>
-                                            </div>
-                                            <p class="text-slate-300 leading-relaxed text-sm whitespace-pre-line m-0"
-                                                x-text="activeProject.statement"></p>
-                                        </div>
-
-                                        <!-- Acceptance Criteria card -->
-                                        <div class="ps-criteria rounded-2xl border p-7"
-                                            style="background:rgba(15,23,42,0.8);border-color:rgba(16,185,129,0.2);">
-                                            <div class="flex items-center gap-2 mb-5">
-                                                <div class="w-1 h-5 rounded-full" style="background:linear-gradient(180deg,#10b981,#06b6d4);"></div>
-                                                <h4 class="text-emerald-400 text-xs font-black uppercase tracking-widest m-0">Acceptance Criteria</h4>
-                                                <span class="ps-count ml-auto text-[10px] font-bold px-2 py-0.5 rounded-full"
-                                                    style="background:rgba(16,185,129,0.12);color:#34d399;"
-                                                    x-text="activeProject.acceptance.length + ' checks'"></span>
-                                            </div>
-                                            <ul class="space-y-3 m-0 p-0 list-none">
-                                                <template x-for="(crit, i) in activeProject.acceptance" :key="crit">
-                                                    <li class="ps-crit-item flex items-start gap-3 text-sm text-slate-300"
-                                                        :style="'animation-delay:' + (0.28 + i * 0.07) + 's'">
-                                                        <div class="ps-crit-icon shrink-0 w-5 h-5 rounded-full flex items-center justify-center mt-0.5"
-                                                            style="background:rgba(16,185,129,0.15);border:1px solid rgba(16,185,129,0.3);">
-                                                            <i class="fas fa-check text-emerald-400" style="font-size:8px;"></i>
-                                                        </div>
-                                                        <span x-text="crit"></span>
-                                                    </li>
-                                                </template>
-                                            </ul>
-                                        </div>
-
+                            <div class="grid grid-cols-1 lg:grid-cols-3 gap-10">
+                                <div class="lg:col-span-2 space-y-8">
+                                    <div class="bg-slate-900 p-8 rounded-3xl border border-slate-800 shadow-xl">
+                                        <h4 class="text-blue-500 text-xs font-black uppercase tracking-widest mb-4">
+                                            Detailed Problem Statement</h4>
+                                        <p class="text-slate-300 leading-relaxed text-sm whitespace-pre-line"
+                                            x-text="activeProject.statement"></p>
                                     </div>
 
-                                    <!-- Right sidebar -->
-                                    <div class="ps-sidebar space-y-4">
-
-                                        <!-- Difficulty meter -->
-                                        <div class="rounded-2xl border p-5"
-                                            style="background:rgba(15,23,42,0.8);border-color:rgba(100,116,139,0.2);">
-                                            <div class="flex items-center gap-2 mb-4">
-                                                <div class="w-1 h-4 rounded-full" style="background:linear-gradient(180deg,#f59e0b,#ef4444);"></div>
-                                                <h4 class="text-slate-400 text-xs font-black uppercase tracking-widest m-0">Difficulty</h4>
-                                            </div>
-                                            <div class="flex items-center gap-2 mb-2">
-                                                <span class="text-sm font-bold text-white capitalize" x-text="activeProject.difficulty"></span>
-                                            </div>
-                                            <div class="w-full rounded-full h-1.5" style="background:rgba(51,65,85,0.8);">
-                                                <div class="ps-difficulty-fill h-1.5 rounded-full"
-                                                    style="background:linear-gradient(90deg,#f59e0b,#ef4444);"
-                                                    :style="'width:' + (activeProject.difficulty==='Basic'?'30':activeProject.difficulty==='Intermediate'?'60':'95') + '%'"></div>
-                                            </div>
-                                            <div class="flex justify-between mt-1">
-                                                <span style="font-size:9px;color:#475569;">Basic</span>
-                                                <span style="font-size:9px;color:#475569;">Advanced</span>
-                                            </div>
-                                        </div>
+                                    <div class="bg-slate-900 p-8 rounded-3xl border border-slate-800 shadow-xl">
+                                        <h4 class="text-emerald-500 text-xs font-black uppercase tracking-widest mb-6">
+                                            Acceptance Criteria</h4>
+                                        <ul class="space-y-4 m-0 p-0">
+                                            <template x-for="crit in activeProject.acceptance" :key="crit">
+                                                <li class="flex items-start gap-3 text-sm text-slate-400">
+                                                    <i class="fas fa-check-circle text-emerald-500 mt-1"></i>
+                                                    <span x-text="crit"></span>
+                                                </li>
+                                            </template>
+                                        </ul>
+                                    </div>
+                                </div>
 
-                                        <!-- Deliverables -->
-                                        <div class="rounded-2xl border p-5"
-                                            style="background:rgba(15,23,42,0.8);border-color:rgba(100,116,139,0.2);">
-                                            <div class="flex items-center gap-2 mb-4">
-                                                <div class="w-1 h-4 rounded-full" style="background:linear-gradient(180deg,#3b82f6,#06b6d4);"></div>
-                                                <h4 class="text-slate-400 text-xs font-black uppercase tracking-widest m-0">Deliverables</h4>
-                                            </div>
-                                            <div class="space-y-2">
-                                                <template x-for="(del, i) in activeProject.deliverables" :key="del">
-                                                    <div class="ps-tag flex items-center gap-2 px-3 py-2 rounded-lg"
-                                                        style="background:rgba(37,99,235,0.1);border:1px solid rgba(59,130,246,0.2);"
-                                                        :style="'animation-delay:' + (0.32 + i*0.08) + 's'">
-                                                        <i class="fas fa-file-code text-blue-400" style="font-size:11px;"></i>
-                                                        <span class="text-xs font-mono text-slate-300" x-text="del"></span>
-                                                    </div>
-                                                </template>
-                                            </div>
+                                <div class="space-y-6">
+                                    <div class="bg-slate-900 p-6 rounded-3xl border border-slate-800">
+                                        <h4 class="text-slate-500 text-xs font-black uppercase mb-4">SDLC Deliverables
+                                        </h4>
+                                        <div class="space-y-3">
+                                            <template x-for="del in activeProject.deliverables" :key="del">
+                                                <div class="flex items-center gap-2 text-xs text-slate-300">
+                                                    <i class="fas fa-file-code text-blue-400"></i>
+                                                    <span x-text="del"></span>
+                                                </div>
+                                            </template>
                                         </div>
-
-                                        <!-- I/O Spec -->
-                                        <div class="ps-inout-card rounded-2xl border p-5"
-                                            style="background:rgba(15,23,42,0.8);border-color:rgba(100,116,139,0.2);">
-                                            <div class="flex items-center gap-2 mb-4">
-                                                <div class="w-1 h-4 rounded-full" style="background:linear-gradient(180deg,#a855f7,#ec4899);"></div>
-                                                <h4 class="text-slate-400 text-xs font-black uppercase tracking-widest m-0">I/O Spec</h4>
-                                            </div>
-                                            <div class="space-y-3">
-                                                <div>
-                                                    <div class="flex items-center gap-1.5 mb-1.5">
-                                                        <i class="fas fa-arrow-right text-blue-400" style="font-size:9px;"></i>
-                                                        <span class="text-[10px] font-black text-slate-500 uppercase tracking-wider">Input</span>
-                                                    </div>
-                                                    <div class="rounded-lg px-3 py-2 font-mono text-[11px] text-slate-400"
-                                                        style="background:rgba(0,0,0,0.4);border:1px solid rgba(51,65,85,0.6);"
-                                                        x-text="activeProject.inputSpec"></div>
+                                    </div>
+                                    <div class="bg-slate-900 p-6 rounded-3xl border border-slate-800">
+                                        <h4 class="text-slate-500 text-xs font-black uppercase mb-4">Input/Output Spec
+                                        </h4>
+                                        <div class="space-y-4">
+                                            <div>
+                                                <div class="text-[10px] font-bold text-slate-600 uppercase mb-1">Inputs
                                                 </div>
-                                                <div>
-                                                    <div class="flex items-center gap-1.5 mb-1.5">
-                                                        <i class="fas fa-arrow-left text-emerald-400" style="font-size:9px;"></i>
-                                                        <span class="text-[10px] font-black text-slate-500 uppercase tracking-wider">Output</span>
-                                                    </div>
-                                                    <div class="rounded-lg px-3 py-2 font-mono text-[11px] text-slate-400"
-                                                        style="background:rgba(0,0,0,0.4);border:1px solid rgba(51,65,85,0.6);"
-                                                        x-text="activeProject.outputSpec"></div>
+                                                <div class="bg-slate-950 p-2 rounded text-[10px] text-slate-400 font-mono"
+                                                    x-text="activeProject.inputSpec"></div>
+                                            </div>
+                                            <div>
+                                                <div class="text-[10px] font-bold text-slate-600 uppercase mb-1">Outputs
                                                 </div>
+                                                <div class="bg-slate-950 p-2 rounded text-[10px] text-slate-400 font-mono"
+                                                    x-text="activeProject.outputSpec"></div>
                                             </div>
                                         </div>
-
-                                        <!-- Quick tip -->
-                                        <div class="ps-tips rounded-2xl border p-4 flex gap-3"
-                                            style="background:rgba(234,179,8,0.05);border-color:rgba(234,179,8,0.2);">
-                                            <i class="fas fa-lightbulb text-yellow-400 mt-0.5 shrink-0" style="font-size:13px;"></i>
-                                            <p class="text-[11px] text-slate-400 leading-relaxed m-0">Read the acceptance criteria carefully before writing any code. Each check maps to a specific expected output.</p>
-                                        </div>
-
                                     </div>
                                 </div>
                             </div>
                         </div>
+
                         <!-- Editor View -->
                         <div x-show="wsTab === 'editor'"
                             class="absolute inset-0 flex flex-col bg-slate-950 overflow-hidden">
@@ -1047,17 +1091,15 @@
                         <button @click="adminTab = 'overview'"
                             :class="adminTab === 'overview' ? 'bg-blue-600 text-white shadow-lg shadow-blue-900/40' : 'text-slate-500 hover:text-slate-300'"
                             class="px-5 py-2 rounded-lg text-[10px] font-black uppercase tracking-widest transition-all">
-                            Overview
+                            <i class="fas fa-chart-line mr-1"></i> Overview
                         </button>
                         <button @click="adminTab = 'submissions'; loadAllSubmissions()"
                             :class="adminTab === 'submissions' ? 'bg-emerald-600 text-white shadow-lg shadow-emerald-900/40' : 'text-slate-500 hover:text-slate-300'"
                             class="px-5 py-2 rounded-lg text-[10px] font-black uppercase tracking-widest transition-all">
-                            Submissions
-                        </button>
-                        <button @click="adminTab = 'analytics'"
-                            :class="adminTab === 'analytics' ? 'bg-indigo-600 text-white shadow-lg shadow-indigo-900/40' : 'text-slate-500 hover:text-slate-300'"
-                            class="px-5 py-2 rounded-lg text-[10px] font-black uppercase tracking-widest transition-all">
-                            Analytics Hub
+                            <i class="fas fa-inbox mr-1"></i> Submissions
+                            <span x-show="allSubmissions.filter(s=>s.status==='pending').length > 0"
+                                class="ml-1.5 bg-amber-500 text-white text-[9px] font-black px-1.5 py-0.5 rounded-full"
+                                x-text="allSubmissions.filter(s=>s.status==='pending').length"></span>
                         </button>
                     </nav>
                 </div>
@@ -1068,229 +1110,258 @@
                 <!-- Tab: Overview (The "WOW" Dashboard) -->
                 <div x-show="adminTab === 'overview'" x-transition class="space-y-10">
                     <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
-                        <!-- Stat Card 1 -->
-                        <div
-                            class="bg-slate-900 border border-slate-800 p-6 rounded-3xl relative overflow-hidden group hover:border-blue-500/50 transition-all shadow-2xl">
-                            <div
-                                class="absolute -right-4 -top-4 text-blue-500/10 text-7xl group-hover:scale-110 transition-transform">
-                                <i class="fas fa-users"></i>
-                            </div>
-                            <div class="text-[10px] font-black text-slate-500 uppercase tracking-widest mb-1">Total
-                                Candidates</div>
-                            <div class="text-4xl font-black text-white" x-text="candidates.length"></div>
-                            <div class="mt-4 flex items-center gap-2 text-[10px] font-bold text-emerald-400">
-                                <i class="fas fa-arrow-up"></i> 12% from last week
+                <!-- Stat Card 1 -->
+                        <div class="bg-slate-900 border border-slate-800 p-6 rounded-3xl relative overflow-hidden group hover:border-blue-500/50 transition-all shadow-2xl">
+                            <div class="absolute -right-4 -top-4 text-blue-500/10 text-7xl group-hover:scale-110 transition-transform"><i class="fas fa-users"></i></div>
+                            <div class="text-[10px] font-black text-slate-500 uppercase tracking-widest mb-1">Total Candidates</div>
+                            <div class="text-4xl font-black text-white" x-text="candidates.length || 0"></div>
+                            <div class="mt-4 flex items-center gap-2 text-[10px] font-bold text-slate-500">
+                                <i class="fas fa-user-check text-emerald-400"></i>
+                                <span x-text="candidates.filter(c=>c.submission_count>0).length + ' active'"></span>
                             </div>
                         </div>
                         <!-- Stat Card 2 -->
-                        <div
-                            class="bg-slate-900 border border-slate-800 p-6 rounded-3xl relative overflow-hidden group hover:border-emerald-500/50 transition-all shadow-2xl">
-                            <div
-                                class="absolute -right-4 -top-4 text-emerald-500/10 text-7xl group-hover:scale-110 transition-transform">
-                                <i class="fas fa-cloud-upload-alt"></i>
-                            </div>
-                            <div class="text-[10px] font-black text-slate-500 uppercase tracking-widest mb-1">Total
-                                Submissions</div>
-                            <div class="text-4xl font-black text-white" x-text="allSubmissions.length"></div>
-                            <div class="mt-4 flex items-center gap-2 text-[10px] font-bold text-blue-400">
-                                <i class="fas fa-sync-alt fa-spin"></i> Real-time tracking
+                        <div class="bg-slate-900 border border-slate-800 p-6 rounded-3xl relative overflow-hidden group hover:border-emerald-500/50 transition-all shadow-2xl">
+                            <div class="absolute -right-4 -top-4 text-emerald-500/10 text-7xl group-hover:scale-110 transition-transform"><i class="fas fa-cloud-upload-alt"></i></div>
+                            <div class="text-[10px] font-black text-slate-500 uppercase tracking-widest mb-1">Total Submissions</div>
+                            <div class="text-4xl font-black text-white" x-text="allSubmissions.length || 0"></div>
+                            <div class="mt-4 flex items-center gap-2 text-[10px] font-bold text-emerald-400">
+                                <i class="fas fa-check-circle"></i>
+                                <span x-text="allSubmissions.filter(s=>s.status==='graded').length + ' graded'"></span>
                             </div>
                         </div>
                         <!-- Stat Card 3 -->
-                        <div
-                            class="bg-slate-900 border border-slate-800 p-6 rounded-3xl relative overflow-hidden group hover:border-amber-500/50 transition-all shadow-2xl">
-                            <div
-                                class="absolute -right-4 -top-4 text-amber-500/10 text-7xl group-hover:scale-110 transition-transform">
-                                <i class="fas fa-clock"></i>
-                            </div>
-                            <div class="text-[10px] font-black text-slate-500 uppercase tracking-widest mb-1">Pending
-                                Review</div>
-                            <div class="text-4xl font-black text-amber-400"
-                                x-text="allSubmissions.filter(s => s.status === 'pending').length"></div>
-                            <div
+                        <div class="bg-slate-900 border border-slate-800 p-6 rounded-3xl relative overflow-hidden group hover:border-amber-500/50 transition-all shadow-2xl cursor-pointer"
+                            @click="adminTab='submissions'; adminSubFilter='pending'; loadAllSubmissions()">
+                            <div class="absolute -right-4 -top-4 text-amber-500/10 text-7xl group-hover:scale-110 transition-transform"><i class="fas fa-clock"></i></div>
+                            <div class="text-[10px] font-black text-slate-500 uppercase tracking-widest mb-1">Pending Review</div>
+                            <div class="text-4xl font-black text-amber-400" x-text="allSubmissions.filter(s=>s.status==='pending').length"></div>
+                            <div x-show="allSubmissions.filter(s=>s.status==='pending').length > 0"
                                 class="mt-4 flex items-center gap-2 text-[10px] font-bold text-amber-500 animate-pulse">
-                                <i class="fas fa-exclamation-circle"></i> Requires attention
+                                <i class="fas fa-exclamation-circle"></i> Click to review
+                            </div>
+                            <div x-show="allSubmissions.filter(s=>s.status==='pending').length === 0"
+                                class="mt-4 flex items-center gap-2 text-[10px] font-bold text-slate-500">
+                                <i class="fas fa-check text-emerald-400"></i> All reviewed
                             </div>
                         </div>
+                        <!-- Stat Card 4 -->
+                        <div class="bg-slate-900 border border-slate-800 p-6 rounded-3xl relative overflow-hidden group hover:border-red-500/50 transition-all shadow-2xl">
+                            <div class="absolute -right-4 -top-4 text-red-500/10 text-7xl group-hover:scale-110 transition-transform"><i class="fas fa-exclamation-triangle"></i></div>
+                            <div class="text-[10px] font-black text-slate-500 uppercase tracking-widest mb-1">Total Violations</div>
+                            <div class="text-4xl font-black text-red-400" x-text="allSubmissions.reduce((a,s)=>a+(s.violation_count||0),0)"></div>
+                            <div class="mt-4 flex items-center gap-2 text-[10px] font-bold text-slate-500">
+                                <i class="fas fa-shield-alt text-red-400"></i>
+                                <span x-text="allSubmissions.filter(s=>(s.violation_count||0)>5).length + ' high-risk'"></span>
+                            </div>
+                        </div>
+                    </div>
 
+                    <!-- Loading spinner for overview -->
+                    <div x-show="submissionsLoading" class="flex items-center justify-center py-12">
+                        <i class="fas fa-circle-notch fa-spin text-blue-400 text-2xl mr-3"></i>
+                        <span class="text-slate-400 text-sm">Loading dashboard data...</span>
                     </div>
 
-                    <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
-                        <!-- Recent Activity -->
+                    <div x-show="!submissionsLoading" class="grid grid-cols-1 lg:grid-cols-3 gap-8">
+                        <!-- Recent Submissions -->
                         <div class="lg:col-span-2 space-y-4">
-                            <h3
-                                class="text-xs font-black text-slate-500 uppercase tracking-widest flex items-center gap-2 ml-1">
-                                <i class="fas fa-history text-blue-500"></i> Recent Platform Activity
+                            <h3 class="text-xs font-black text-slate-500 uppercase tracking-widest flex items-center gap-2 ml-1">
+                                <i class="fas fa-history text-blue-500"></i> Recent Submissions
                             </h3>
                             <div class="bg-slate-900 border border-slate-800 rounded-3xl overflow-hidden shadow-2xl">
-                                <div class="p-6 space-y-6">
-                                    <template x-for="sub in allSubmissions.slice(0, 5)" :key="sub.id">
-                                        <div class="flex items-center justify-between group cursor-pointer"
-                                            @click="adminTab = 'submissions'; openGrading(sub)">
-                                            <div class="flex items-center gap-4">
-                                                <div
-                                                    class="w-10 h-10 rounded-xl bg-slate-800 flex items-center justify-center text-slate-400 group-hover:bg-blue-600 group-hover:text-white transition-all">
-                                                    <i class="fas"
-                                                        :class="sub.status === 'graded' ? 'fa-check-double text-emerald-400' : 'fa-clock text-amber-400'"></i>
+                                <div class="p-6 space-y-3">
+                                    <template x-for="sub in allSubmissions.slice(0,8)" :key="sub.id">
+                                        <div class="flex items-center justify-between group cursor-pointer p-3 rounded-2xl hover:bg-slate-800/50 transition-all"
+                                            @click="adminTab='submissions'; openGrading(sub)">
+                                            <div class="flex items-center gap-3">
+                                                <div class="w-9 h-9 rounded-xl flex items-center justify-center"
+                                                    :class="sub.status==='graded' ? 'bg-emerald-600/20 text-emerald-400' : 'bg-amber-600/20 text-amber-400'">
+                                                    <i class="fas text-sm" :class="sub.status==='graded' ? 'fa-check-double' : 'fa-clock'"></i>
                                                 </div>
                                                 <div>
-                                                    <div class="text-sm font-bold text-white"
-                                                        x-text="sub.candidateName"></div>
-                                                    <div class="text-[10px] text-slate-500"
-                                                        x-text="sub.projectTitle + ' ΓÇó Day ' + sub.day"></div>
+                                                    <div class="text-sm font-bold text-white" x-text="sub.candidate_name || 'ΓÇö'"></div>
+                                                    <div class="text-[10px] text-slate-500" x-text="(sub.project_title||'ΓÇö') + ' ┬╖ ' + (sub.topic_name||'')"></div>
                                                 </div>
                                             </div>
-                                            <div class="text-right">
-                                                <div class="text-[10px] font-black uppercase text-blue-400"
-                                                    x-text="sub.status"></div>
-                                                <div class="text-[9px] text-slate-600"
-                                                    x-text="new Date(sub.submittedAt).toLocaleTimeString()"></div>
+                                            <div class="text-right shrink-0">
+                                                <span class="text-[10px] font-black uppercase px-2 py-0.5 rounded-lg"
+                                                    :class="sub.status==='graded' ? 'bg-emerald-500/10 text-emerald-400' : 'bg-amber-500/10 text-amber-400'"
+                                                    x-text="sub.status"></span>
+                                                <div class="text-[9px] text-slate-600 mt-1"
+                                                    x-text="sub.submitted_at ? new Date(sub.submitted_at).toLocaleString('en-IN',{day:'2-digit',month:'short',hour:'2-digit',minute:'2-digit'}) : 'ΓÇö'"></div>
                                             </div>
                                         </div>
                                     </template>
-                                    <template x-if="allSubmissions.length === 0">
-                                        <div class="text-center py-10 text-slate-600 italic text-xs">No recent
-                                            submissions to display.</div>
-                                    </template>
+                                    <div x-show="allSubmissions.length === 0 && !submissionsLoading"
+                                        class="text-center py-10 text-slate-600 italic text-xs">
+                                        <i class="fas fa-inbox text-3xl mb-3 block"></i>No submissions yet.
+                                    </div>
+                                </div>
+                                <div x-show="allSubmissions.length > 8" class="border-t border-slate-800 p-4 text-center">
+                                    <button @click="adminTab='submissions'; loadAllSubmissions()"
+                                        class="text-xs text-blue-400 hover:text-blue-300 font-bold transition-colors">
+                                        View all <span x-text="allSubmissions.length"></span> submissions ΓåÆ
+                                    </button>
                                 </div>
                             </div>
                         </div>
 
                         <!-- Quick Actions -->
                         <div class="space-y-4">
-                            <h3
-                                class="text-xs font-black text-slate-500 uppercase tracking-widest flex items-center gap-2 ml-1">
-                                <i class="fas fa-bolt text-amber-500"></i> Fast Track Actions
+                            <h3 class="text-xs font-black text-slate-500 uppercase tracking-widest flex items-center gap-2 ml-1">
+                                <i class="fas fa-bolt text-amber-500"></i> Quick Actions
                             </h3>
                             <div class="bg-slate-900 border border-slate-800 p-6 rounded-3xl shadow-2xl space-y-3">
                                 <button @click="loadAllSubmissions(); adminTab='submissions'; adminSubFilter='pending'"
                                     class="w-full bg-blue-600/10 hover:bg-blue-600/20 text-blue-400 border border-blue-600/30 p-4 rounded-2xl text-xs font-bold transition-all flex items-center justify-between">
-                                    Grade Pending Submissions <i class="fas fa-chevron-right text-[10px]"></i>
+                                    <span>Review Pending
+                                        <span x-show="allSubmissions.filter(s=>s.status==='pending').length > 0"
+                                            class="ml-1 bg-amber-500 text-white text-[9px] px-1.5 py-0.5 rounded-full font-black"
+                                            x-text="allSubmissions.filter(s=>s.status==='pending').length"></span>
+                                    </span>
+                                    <i class="fas fa-chevron-right text-[10px]"></i>
                                 </button>
-                                <button @click="downloadAllReports()" :disabled="!allSubmissions || !allSubmissions.length"
+                                <button @click="downloadAllReports()" :disabled="!allSubmissions.length"
                                     class="w-full bg-emerald-600/10 hover:bg-emerald-600/20 text-emerald-400 border border-emerald-600/30 p-4 rounded-2xl text-xs font-bold transition-all flex items-center justify-between disabled:opacity-40">
                                     Export All (.xlsx) <i class="fas fa-file-excel text-[10px]"></i>
                                 </button>
                                 <button @click="sendWeeklyReport()" :disabled="weeklyReportSending"
                                     class="w-full bg-violet-600/10 hover:bg-violet-600/20 text-violet-400 border border-violet-600/30 p-4 rounded-2xl text-xs font-bold transition-all flex items-center justify-between disabled:opacity-50">
-                                    <span x-text="weeklyReportSending ? 'Sending...' : 'Send Weekly Report to Management'"></span>
-                                    <i class="fas fa-paper-plane text-[10px]"></i>
+                                    <span x-text="weeklyReportSending ? 'Sending...' : 'Send Weekly Report'"></span>
+                                    <i class="fas" :class="weeklyReportSending ? 'fa-circle-notch fa-spin' : 'fa-envelope'"></i>
+                                </button>
+                                <button @click="loadAllSubmissions()"
+                                    class="w-full bg-slate-800 hover:bg-slate-700 text-slate-400 border border-slate-700 p-4 rounded-2xl text-xs font-bold transition-all flex items-center justify-between">
+                                    Refresh Data <i class="fas fa-sync-alt text-[10px]"></i>
                                 </button>
+                                <button @click="runDbDiagnostics()"
+                                    :disabled="dbDiagLoading"
+                                    class="w-full bg-slate-800 hover:bg-slate-700 text-slate-400 border border-slate-700 p-4 rounded-2xl text-xs font-bold transition-all flex items-center justify-between disabled:opacity-50">
+                                    <span x-text="dbDiagLoading ? 'Checking DB...' : 'Check DB Health'"></span>
+                                    <i class="fas" :class="dbDiagLoading ? 'fa-circle-notch fa-spin' : 'fa-database'"></i>
+                                </button>
+                                <p class="text-[10px] text-slate-600 px-1 pt-1">
+                                    <i class="fas fa-calendar-check mr-1 text-violet-500"></i>
+                                    Auto-report every <span class="text-slate-500 font-bold">Friday 6 PM IST</span>
+                                </p>
                             </div>
                         </div>
                     </div>
                 </div>
 
-                <!-- Tab: Submissions Review Hub -->
+                <!-- Tab: Submissions -->
                 <div x-show="adminTab === 'submissions'" x-transition>
                     <div x-show="!gradingSubmission" class="space-y-8">
-                        <div class="flex justify-between items-end">
+                        <div class="flex justify-between items-end flex-wrap gap-4">
                             <div>
-                                <h2 class="text-3xl font-black text-white tracking-tighter">Review <span
-                                        class="text-emerald-500">Submissions</span></h2>
-                                <p class="text-xs text-slate-500 mt-1">Industrial-grade code evaluation & feedback
-                                    system.</p>
+                                <h2 class="text-3xl font-black text-white tracking-tighter">Candidate <span class="text-emerald-500">Submissions</span></h2>
+                                <p class="text-xs text-slate-500 mt-1">
+                                    <span x-text="allSubmissions.length"></span> total ┬╖
+                                    <span class="text-amber-400" x-text="allSubmissions.filter(s=>s.status==='pending').length"></span> pending ┬╖
+                                    <span class="text-emerald-400" x-text="allSubmissions.filter(s=>s.status==='graded').length"></span> graded
+                                </p>
                             </div>
-                            <div class="flex items-center gap-3">
-                                <!-- Enhanced Search -->
+                            <div class="flex items-center gap-3 flex-wrap">
+                                <button @click="downloadAllReports()" :disabled="!allSubmissions.length"
+                                    class="bg-emerald-700 hover:bg-emerald-600 disabled:opacity-40 text-white px-4 py-2.5 rounded-xl text-xs border border-emerald-600 transition-all font-black flex items-center gap-2">
+                                    <i class="fas fa-file-excel"></i> Export All (.xlsx)
+                                </button>
                                 <div class="relative">
-                                    <i
-                                        class="fas fa-search absolute left-4 top-1/2 -translate-y-1/2 text-slate-500 text-xs"></i>
-                                    <input type="text" x-model="adminSearch" placeholder="Search candidate..."
-                                        class="bg-slate-900 border border-slate-800 text-slate-300 text-xs rounded-xl pl-10 pr-4 py-2.5 outline-none focus:ring-2 focus:ring-blue-500 w-64 transition-all">
+                                    <i class="fas fa-search absolute left-4 top-1/2 -translate-y-1/2 text-slate-500 text-xs"></i>
+                                    <input type="text" x-model="adminSearch" placeholder="Search name or email..."
+                                        class="bg-slate-900 border border-slate-800 text-slate-300 text-xs rounded-xl pl-10 pr-4 py-2.5 outline-none focus:ring-2 focus:ring-blue-500 w-56 transition-all">
                                 </div>
-                                <select x-model="adminDayFilter" @change="loadAllSubmissions()"
-                                    class="bg-slate-900 border border-slate-800 text-slate-300 text-xs rounded-xl px-4 py-2.5 outline-none focus:ring-2 focus:ring-blue-500">
-                                    <option value="">All Days</option>
-                                    <option value="1">Day 1</option>
-                                    <option value="2">Day 2</option>
-                                    <option value="3">Day 3</option>
-                                    <option value="4">Day 4</option>
-                                    <option value="5">Day 5</option>
-                                </select>
                                 <select x-model="adminSubFilter" @change="loadAllSubmissions()"
                                     class="bg-slate-900 border border-slate-800 text-slate-300 text-xs rounded-xl px-4 py-2.5 outline-none focus:ring-2 focus:ring-blue-500">
                                     <option value="all">All Status</option>
                                     <option value="pending">Pending</option>
                                     <option value="graded">Graded</option>
                                 </select>
-                                <button @click="loadAllSubmissions()"
-                                    class="bg-slate-800 hover:bg-slate-700 text-slate-300 px-3.5 py-2.5 rounded-xl text-xs border border-slate-700 transition-all">
-                                    <i class="fas fa-sync-alt"></i>
+                                <button @click="loadAllSubmissions()" :disabled="submissionsLoading"
+                                    class="bg-slate-800 hover:bg-slate-700 disabled:opacity-50 text-slate-300 px-3.5 py-2.5 rounded-xl text-xs border border-slate-700 transition-all">
+                                    <i class="fas" :class="submissionsLoading ? 'fa-circle-notch fa-spin' : 'fa-sync-alt'"></i>
                                 </button>
                             </div>
                         </div>
 
-                        <div x-show="allSubmissions.filter(s => !adminSearch || s.candidateName.toLowerCase().includes(adminSearch.toLowerCase()) || s.email.toLowerCase().includes(adminSearch.toLowerCase())).length === 0"
+                        <!-- Loading -->
+                        <div x-show="submissionsLoading" class="flex items-center justify-center py-20">
+                            <i class="fas fa-circle-notch fa-spin text-blue-400 text-3xl mr-4"></i>
+                            <span class="text-slate-400">Loading submissions...</span>
+                        </div>
+
+                        <!-- Error -->
+                        <div x-show="submissionsError && !submissionsLoading"
+                            class="bg-red-900/20 border border-red-700/40 rounded-2xl p-6 flex items-center justify-between">
+                            <div class="flex items-center gap-3 text-red-400">
+                                <i class="fas fa-exclamation-triangle"></i>
+                                <span class="text-sm" x-text="submissionsError"></span>
+                            </div>
+                            <button @click="loadAllSubmissions()"
+                                class="text-xs text-red-400 hover:text-red-300 font-bold border border-red-700/40 px-3 py-1.5 rounded-lg">
+                                <i class="fas fa-redo mr-1"></i> Retry
+                            </button>
+                        </div>
+
+                        <!-- Empty -->
+                        <div x-show="!submissionsLoading && !submissionsError && filteredSubmissions.length === 0"
                             class="text-center py-20 text-slate-600 bg-slate-900 border border-slate-800 rounded-3xl">
-                            <i class="fas fa-clipboard-list text-5xl mb-4"></i>
-                            <p class="text-lg font-bold">No matching submissions</p>
+                            <i class="fas fa-inbox text-5xl mb-4 block"></i>
+                            <p class="text-lg font-bold">No submissions found</p>
                             <p class="text-xs text-slate-700 mt-2">Try adjusting your filters or search query.</p>
                         </div>
 
-                        <div x-show="allSubmissions.length > 0"
+                        <!-- Table -->
+                        <div x-show="!submissionsLoading && !submissionsError && filteredSubmissions.length > 0"
                             class="bg-slate-900 rounded-3xl border border-slate-800 overflow-hidden shadow-2xl">
                             <table class="w-full text-left">
                                 <thead>
                                     <tr class="bg-slate-800/50">
-                                        <th class="px-5 py-4 text-[10px] font-black text-slate-500 uppercase">
-                                            Candidate</th>
-                                        <th class="px-5 py-4 text-[10px] font-black text-slate-500 uppercase">
-                                            Problem Statement Name</th>
-                                        <th class="px-5 py-4 text-[10px] font-black text-slate-500 uppercase">
-                                            Trainer Feedback</th>
-                                        <th
-                                            class="px-5 py-4 text-[10px] font-black text-slate-500 uppercase text-center">
-                                            Grading</th>
-                                        <th
-                                            class="px-5 py-4 text-[10px] font-black text-slate-500 uppercase text-center">
-                                            Status</th>
-                                        <th
-                                            class="px-5 py-4 text-[10px] font-black text-slate-500 uppercase text-right">
-                                            Action</th>
+                                        <th class="px-5 py-4 text-[10px] font-black text-slate-500 uppercase">Candidate</th>
+                                        <th class="px-5 py-4 text-[10px] font-black text-slate-500 uppercase">Problem</th>
+                                        <th class="px-5 py-4 text-[10px] font-black text-slate-500 uppercase text-center">Score</th>
+                                        <th class="px-5 py-4 text-[10px] font-black text-slate-500 uppercase text-center">Violations</th>
+                                        <th class="px-5 py-4 text-[10px] font-black text-slate-500 uppercase text-center">Status</th>
+                                        <th class="px-5 py-4 text-[10px] font-black text-slate-500 uppercase text-center">Submitted</th>
+                                        <th class="px-5 py-4 text-[10px] font-black text-slate-500 uppercase text-right">Action</th>
                                     </tr>
                                 </thead>
                                 <tbody>
-                                    <template
-                                        x-for="sub in allSubmissions.filter(s => !adminSearch || s.candidateName.toLowerCase().includes(adminSearch.toLowerCase()) || s.email.toLowerCase().includes(adminSearch.toLowerCase()))"
-                                        :key="sub.id">
-                                        <tr
-                                            class="border-t border-slate-800/50 hover:bg-slate-800/30 transition-colors">
+                                    <template x-for="sub in filteredSubmissions" :key="sub.id">
+                                        <tr class="border-t border-slate-800/50 hover:bg-slate-800/30 transition-colors">
                                             <td class="px-5 py-4">
-                                                <div class="text-sm font-bold text-white" x-text="sub.candidateName">
-                                                </div>
+                                                <div class="text-sm font-bold text-white" x-text="sub.candidate_name || 'ΓÇö'"></div>
                                                 <div class="text-[10px] text-slate-500" x-text="sub.email"></div>
                                             </td>
                                             <td class="px-5 py-4">
-                                                <div class="text-xs font-bold text-slate-300" x-text="sub.projectTitle">
-                                                </div>
-                                                <div class="text-[10px] text-slate-500"
-                                                    x-text="'Day ' + sub.day + ' ΓÇó ' + sub.topicName"></div>
+                                                <div class="text-xs font-bold text-slate-300" x-text="sub.project_title || 'ΓÇö'"></div>
+                                                <div class="text-[10px] text-slate-600" x-text="sub.topic_name || ''"></div>
                                             </td>
-                                            <td class="px-5 py-4 text-xs text-slate-400 italic">
-                                                <div class="max-w-[200px] truncate" :title="sub.grade?.comments"
-                                                    x-text="sub.grade ? sub.grade.comments : 'Not yet graded'"></div>
+                                            <td class="px-5 py-4 text-center">
+                                                <span class="text-sm font-black"
+                                                    :class="(sub.auto_score||0)>=70 ? 'text-emerald-400' : 'text-amber-400'"
+                                                    x-text="(sub.auto_score||0) + '%'"></span>
+                                                <div class="text-[9px] text-slate-600"
+                                                    x-text="(sub.tests_passed||0) + '/' + (sub.tests_total||0) + ' tests'"></div>
                                             </td>
                                             <td class="px-5 py-4 text-center">
-                                                <div class="text-sm font-black"
-                                                    :class="sub.grade ? 'text-emerald-400' : 'text-slate-600'"
-                                                    x-text="sub.grade ? sub.grade.compositeScore + '%' : 'ΓÇö'"></div>
-                                                <div class="text-[9px] text-slate-500"
-                                                    x-text="'Auto: ' + sub.autoScore + '%'"></div>
+                                                <span class="text-sm font-black"
+                                                    :class="(sub.violation_count||0)>5 ? 'text-red-400' : (sub.violation_count||0)>0 ? 'text-amber-400' : 'text-slate-500'"
+                                                    x-text="sub.violation_count||0"></span>
                                             </td>
                                             <td class="px-5 py-4 text-center">
-                                                <span class="text-[8px] font-black uppercase px-2 py-1 rounded-md"
-                                                    :class="sub.status === 'graded' ? 'bg-emerald-500/20 text-emerald-400' : 'bg-amber-500/20 text-amber-400 border border-amber-500/30'"
+                                                <span class="text-[10px] font-black uppercase px-3 py-1 rounded-full"
+                                                    :class="sub.status==='graded' ? 'bg-emerald-500/20 text-emerald-400' : 'bg-amber-500/20 text-amber-400'"
                                                     x-text="sub.status"></span>
                                             </td>
+                                            <td class="px-5 py-4 text-center text-[10px] text-slate-500"
+                                                x-text="sub.submitted_at ? new Date(sub.submitted_at).toLocaleString('en-IN',{day:'2-digit',month:'short',hour:'2-digit',minute:'2-digit'}) : 'ΓÇö'">
+                                            </td>
                                             <td class="px-5 py-4 text-right">
                                                 <button @click="openGrading(sub)"
-                                                    class="text-[9px] font-black uppercase tracking-widest px-4 py-2 rounded-xl border transition-all"
-                                                    :class="sub.status === 'graded' ? 'bg-slate-800 hover:bg-slate-700 text-slate-300 border-slate-700' : 'bg-emerald-600 hover:bg-emerald-500 text-white border-emerald-500 shadow-lg shadow-emerald-900/20'">
-                                                    <i class="fas"
-                                                        :class="sub.status === 'graded' ? 'fa-eye' : 'fa-pen-nib'"></i>
-                                                    <span
-                                                        x-text="sub.status === 'graded' ? 'Review' : 'Grade Now'"></span>
+                                                    class="bg-blue-600/10 hover:bg-blue-600/30 text-blue-400 border border-blue-600/30 px-4 py-2 rounded-xl text-[10px] font-black transition-all">
+                                                    <i class="fas fa-eye mr-1"></i> View Report
                                                 </button>
                                             </td>
                                         </tr>
@@ -1298,237 +1369,116 @@
                                 </tbody>
                             </table>
                         </div>
-                    </div>
 
-                    <!-- Grading View (Integrated) -->
-                    <div x-show="gradingSubmission" x-transition class="space-y-6">
-                        <div class="flex items-center gap-4">
-                            <button @click="gradingSubmission = null; gradingDetail = null"
-                                class="w-10 h-10 bg-slate-900 border border-slate-800 text-slate-500 hover:text-white rounded-xl flex items-center justify-center transition-all">
-                                <i class="fas fa-arrow-left"></i>
-                            </button>
-                            <div>
-                                <h3 class="text-xl font-black text-white tracking-tight">Grading: <span
-                                        class="text-blue-500" x-text="gradingSubmission?.candidateName"></span></h3>
-                                <p class="text-[10px] text-slate-500 uppercase tracking-widest font-bold"
-                                    x-text="gradingSubmission?.projectTitle"></p>
-                            </div>
-                        </div>
 
-                        <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
-                            <!-- Code Viewer -->
-                            <div
-                                class="bg-slate-900 border border-slate-800 rounded-3xl overflow-hidden shadow-2xl h-[700px] flex flex-col">
-                                <div
-                                    class="px-6 py-4 bg-slate-800/50 flex justify-between items-center border-b border-slate-800">
-                                    <div
-                                        class="text-[10px] font-black text-slate-400 uppercase tracking-widest flex items-center gap-2">
-                                        <i class="fas fa-terminal text-blue-400"></i> Source Code Artifacts
-                                    </div>
-                                    <div class="flex gap-2">
-                                        <div class="w-3 h-3 rounded-full bg-red-500/50"></div>
-                                        <div class="w-3 h-3 rounded-full bg-amber-500/50"></div>
-                                        <div class="w-3 h-3 rounded-full bg-emerald-500/50"></div>
-                                    </div>
-                                </div>
-                                <div class="flex-1 overflow-hidden flex flex-col">
-                                    <template x-if="gradingDetail">
-                                        <div class="flex-1 flex flex-col overflow-hidden">
-                                            <template x-for="file in gradingDetail.code" :key="file.name">
-                                                <div class="flex-1 flex flex-col overflow-hidden">
-                                                    <div
-                                                        class="px-6 py-2 bg-slate-950/80 text-[10px] font-bold text-blue-400 flex items-center gap-2 border-b border-slate-800">
-                                                        <i class="fas fa-file-code"></i> <span
-                                                            x-text="file.name"></span>
-                                                    </div>
-                                                    <pre class="flex-1 p-6 text-xs code-font text-slate-300 overflow-auto bg-slate-950 custom-scrollbar leading-relaxed"
-                                                        x-text="file.content"></pre>
-                                                </div>
-                                            </template>
-                                        </div>
-                                    </template>
-                                    <div x-show="!gradingDetail"
-                                        class="flex-1 flex items-center justify-center text-slate-600 bg-slate-950">
-                                        <div class="text-center">
-                                            <i class="fas fa-spinner fa-spin text-3xl mb-4 text-blue-500/50"></i>
-                                            <p class="text-xs font-bold uppercase tracking-widest">Hydrating Source...
-                                            </p>
-                                        </div>
-                                    </div>
-                                </div>
-                            </div>
 
-                            <!-- Grading Controls -->
-                            <div class="space-y-6 flex flex-col">
-                                <div class="bg-slate-900 border border-slate-800 p-8 rounded-3xl shadow-2xl flex-1">
-                                    <h4
-                                        class="text-xs font-black text-emerald-400 uppercase tracking-widest mb-8 flex items-center gap-2">
-                                        <i class="fas fa-tasks"></i> Rubric Assessment
-                                    </h4>
-
-                                    <div class="grid grid-cols-1 gap-8">
-                                        <template x-for="metric in ['correctness', 'codeQuality', 'logic', 'style']">
-                                            <div class="bg-slate-950/50 p-6 rounded-2xl border border-slate-800/50">
-                                                <div class="flex justify-between items-center mb-4">
-                                                    <label
-                                                        class="text-[10px] font-black text-slate-400 uppercase tracking-widest"
-                                                        x-text="metric.replace(/([A-Z])/g, ' $1')"></label>
-                                                    <div
-                                                        class="flex items-center gap-2 bg-slate-900 px-3 py-1 rounded-lg border border-slate-800">
-                                                        <span class="text-lg font-black text-blue-500"
-                                                            x-text="gradeScores[metric]"></span>
-                                                        <span class="text-[10px] text-slate-600 uppercase">/ 10</span>
-                                                    </div>
-                                                </div>
-                                                <input type="range" min="0" max="10" step="1"
-                                                    x-model.number="gradeScores[metric]"
-                                                    class="w-full h-1.5 bg-slate-800 rounded-lg appearance-none cursor-pointer accent-blue-600">
-                                            </div>
-                                        </template>
-                                    </div>
+    </div>
+    </div>
 
-                                    <!-- Comment Area -->
-                                    <div class="mt-8">
-                                        <h4
-                                            class="text-[10px] font-black text-slate-500 uppercase tracking-widest mb-4 flex items-center gap-2">
-                                            <i class="fas fa-comment-dots text-indigo-400"></i> Trainer Feedback &
-                                            Insights
-                                        </h4>
-                                        <textarea x-model="gradeComments" rows="4"
-                                            placeholder="Provide actionable feedback for the candidate..."
-                                            class="w-full bg-slate-950 border border-slate-800 rounded-2xl p-6 text-sm text-slate-300 outline-none focus:ring-2 focus:ring-emerald-500 resize-none code-font placeholder:text-slate-700"></textarea>
-                                    </div>
+    <!-- DB Diagnostics Modal -->
+    <div x-show="showDbDiag" x-cloak x-transition
+        class="fixed inset-0 z-[400] bg-slate-950/95 backdrop-blur-md flex items-center justify-center p-6">
+        <div class="bg-slate-900 border border-slate-700 rounded-3xl p-8 max-w-2xl w-full shadow-2xl max-h-[80vh] overflow-y-auto">
+            <div class="flex justify-between items-center mb-6">
+                <h2 class="text-lg font-black text-white flex items-center gap-2">
+                    <i class="fas fa-database text-blue-400"></i> Database Health Check
+                </h2>
+                <button @click="showDbDiag = false" class="text-slate-500 hover:text-white transition-colors">
+                    <i class="fas fa-times"></i>
+                </button>
+            </div>
+            <div x-show="dbDiagResult" class="space-y-4">
+                <!-- Summary -->
+                <div class="grid grid-cols-3 gap-4">
+                    <div class="bg-slate-800 rounded-2xl p-4 text-center">
+                        <div class="text-2xl font-black text-white" x-text="dbDiagResult?.submissions?.total || 0"></div>
+                        <div class="text-[10px] text-slate-500 uppercase tracking-widest mt-1">Total Submissions</div>
+                    </div>
+                    <div class="bg-slate-800 rounded-2xl p-4 text-center">
+                        <div class="text-2xl font-black text-white" x-text="dbDiagResult?.candidates || 0"></div>
+                        <div class="text-[10px] text-slate-500 uppercase tracking-widest mt-1">Candidates</div>
+                    </div>
+                    <div class="bg-slate-800 rounded-2xl p-4 text-center">
+                        <div class="text-2xl font-black text-white" x-text="dbDiagResult?.solvedProblems || 0"></div>
+                        <div class="text-[10px] text-slate-500 uppercase tracking-widest mt-1">Problems Solved</div>
+                    </div>
+                </div>
+                <!-- Status breakdown -->
+                <div class="bg-slate-800 rounded-2xl p-4">
+                    <div class="text-[10px] font-black text-slate-500 uppercase tracking-widest mb-3">By Status</div>
+                    <template x-for="s in (dbDiagResult?.submissions?.byStatus || [])" :key="s.status">
+                        <div class="flex justify-between items-center py-1">
+                            <span class="text-xs font-bold capitalize"
+                                :class="s.status==='graded' ? 'text-emerald-400' : 'text-amber-400'"
+                                x-text="s.status"></span>
+                            <span class="text-xs font-black text-white" x-text="s.count"></span>
+                        </div>
+                    </template>
+                </div>
+                <!-- Recent 20 submissions -->
+                <div class="bg-slate-800 rounded-2xl p-4">
+                    <div class="text-[10px] font-black text-slate-500 uppercase tracking-widest mb-3">Last 20 Submissions in DB</div>
+                    <div class="space-y-2 max-h-64 overflow-y-auto">
+                        <template x-for="r in (dbDiagResult?.submissions?.recent || [])" :key="r.id">
+                            <div class="flex items-center justify-between text-[10px] py-1.5 border-b border-slate-700/50">
+                                <div>
+                                    <span class="font-bold text-white" x-text="r.candidate_name || 'ΓÇö'"></span>
+                                    <span class="text-slate-500 ml-2" x-text="r.email"></span>
                                 </div>
-
-                                <!-- Summary & Submit -->
-                                <div
-                                    class="bg-slate-900 border border-slate-800 p-8 rounded-3xl shadow-2xl flex items-center justify-between gap-10">
-                                    <div class="flex items-center gap-8">
-                                        <div class="text-center">
-                                            <div
-                                                class="text-[9px] text-slate-600 uppercase font-black tracking-widest mb-1">
-                                                Composite</div>
-                                            <div class="text-3xl font-black text-white"
-                                                x-text="Math.round((gradingSubmission?.autoScore || 0) * 0.2 + ((gradeScores.correctness + gradeScores.codeQuality + gradeScores.logic + gradeScores.style) / 4) * 10 * 0.8) + '%'">
-                                            </div>
-                                        </div>
-                                        <div class="w-px h-10 bg-slate-800"></div>
-                                        <div>
-                                            <div class="text-[9px] text-slate-600 uppercase font-bold">Auto (20%): <span
-                                                    class="text-blue-400"
-                                                    x-text="(gradingSubmission?.autoScore || 0) + '%'"></span></div>
-                                            <div class="text-[9px] text-slate-600 uppercase font-bold">Manual (80%):
-                                                <span class="text-emerald-400"
-                                                    x-text="Math.round(((gradeScores.correctness + gradeScores.codeQuality + gradeScores.logic + gradeScores.style) / 4) * 10) + '%'"></span>
-                                            </div>
-                                        </div>
-                                    </div>
-                                    <button @click="submitGrade()" :disabled="isGrading"
-                                        class="px-10 py-5 bg-gradient-to-r from-emerald-600 to-teal-600 hover:from-emerald-500 hover:to-teal-500 disabled:opacity-50 text-white rounded-2xl font-black text-sm transition-all shadow-xl shadow-emerald-900/40 flex items-center justify-center gap-3">
-                                        <i class="fas" :class="isGrading ? 'fa-sync fa-spin' : 'fa-check-circle'"></i>
-                                        <span x-text="isGrading ? 'Finalizing...' : 'Submit Grade'"></span>
-                                    </button>
+                                <div class="text-right shrink-0 ml-4">
+                                    <div class="text-slate-400" x-text="r.project_title || 'ΓÇö'"></div>
+                                    <div class="text-slate-600" x-text="r.submitted_at ? new Date(r.submitted_at).toLocaleString('en-IN') : 'ΓÇö'"></div>
                                 </div>
+                                <span class="ml-3 text-[9px] font-black uppercase px-2 py-0.5 rounded-full shrink-0"
+                                    :class="r.status==='graded' ? 'bg-emerald-500/20 text-emerald-400' : 'bg-amber-500/20 text-amber-400'"
+                                    x-text="r.status"></span>
                             </div>
-                        </div>
-                    </div>
-                </div>
-
-                <!-- Tab: Analytics Hub (Legacy Data) -->
-                <div x-show="adminTab === 'analytics'" x-transition class="space-y-8">
-                    <div>
-                        <h2 class="text-3xl font-black text-white tracking-tighter">Candidate <span
-                                class="text-indigo-500">Analytics</span></h2>
-                        <p class="text-xs text-slate-500 mt-1">Holistic performance tracking and behavioral insights.
-                        </p>
-                    </div>
-
-                    <div class="bg-slate-900 rounded-3xl border border-slate-800 overflow-hidden shadow-2xl">
-                        <table class="w-full text-left">
-                            <thead>
-                                <tr class="bg-slate-800/50">
-                                    <th class="px-6 py-4 text-[10px] font-black text-slate-500 uppercase">Profile</th>
-                                    <th class="px-6 py-4 text-[10px] font-black text-slate-500 uppercase text-center">
-                                        Completed</th>
-                                    <th class="px-6 py-4 text-[10px] font-black text-slate-500 uppercase">Progress</th>
-                                    <th class="px-6 py-4 text-[10px] font-black text-slate-500 uppercase text-center">
-                                        Time</th>
-                                    <th class="px-6 py-4 text-[10px] font-black text-slate-500 uppercase text-right">
-                                        Report</th>
-                                </tr>
-                            </thead>
-                            <tbody>
-                                <template x-for="c in candidates" :key="c.code">
-                            <tbody class="group border-t border-slate-800/50">
-                                <tr class="hover:bg-slate-800/30 transition-colors">
-                                    <td class="px-6 py-4">
-                                        <div class="text-sm font-bold text-white" x-text="c.name"></div>
-                                        <div class="text-[10px] text-slate-500" x-text="c.email"></div>
-                                    </td>
-                                    <td class="px-6 py-4 text-center font-bold text-slate-300"
-                                        x-text="c.completed + ' / 40'"></td>
-                                    <td class="px-6 py-4 w-48">
-                                        <div class="flex items-center gap-3">
-                                            <div class="flex-1 h-2 bg-slate-800 rounded-full overflow-hidden">
-                                                <div class="bg-indigo-500 h-full transition-all"
-                                                    :style="'width: ' + c.progress + '%'"></div>
-                                            </div>
-                                            <span class="text-[10px] font-bold text-slate-500"
-                                                x-text="c.progress + '%'"></span>
-                                        </div>
-                                    </td>
-                                    <td class="px-6 py-4 text-center font-bold text-blue-400" x-text="c.hours + 'h'">
-                                    </td>
-                                    <td class="px-6 py-4 text-right">
-                                        <button @click="c.showDetails = !c.showDetails"
-                                            class="bg-slate-800 hover:bg-slate-700 text-slate-300 px-4 py-2 rounded-xl border border-slate-700 transition-all text-[10px] font-black uppercase tracking-widest">
-                                            Insights
-                                        </button>
-                                    </td>
-                                </tr>
-                                <tr x-show="c.showDetails" x-transition class="bg-slate-950/50">
-                                    <td colspan="5" class="px-10 py-8">
-                                        <div class="grid grid-cols-2 gap-16">
-                                            <div class="space-y-4">
-                                                <h5
-                                                    class="text-[10px] font-black text-slate-500 uppercase tracking-widest">
-                                                    Strengths & Topics Mastery</h5>
-                                                <div class="flex flex-wrap gap-2">
-                                                    <template x-for="st in c.strong" :key="st">
-                                                        <div
-                                                            class="bg-emerald-500/10 border border-emerald-500/30 text-emerald-500 px-4 py-1.5 rounded-xl text-[10px] font-black flex items-center gap-2">
-                                                            <i class="fas fa-check-circle text-[8px]"></i> <span
-                                                                x-text="st"></span>
-                                                        </div>
-                                                    </template>
-                                                </div>
-                                            </div>
-                                            <div class="space-y-4">
-                                                <h5
-                                                    class="text-[10px] font-black text-slate-500 uppercase tracking-widest">
-                                                    Growth Recommendations</h5>
-                                                <p class="text-xs text-slate-400 italic leading-relaxed">"Candidate
-                                                    shows strong aptitude in <span class="text-blue-400 font-bold"
-                                                        x-text="c.strong[0]"></span>. Suggested roadmap: Advanced
-                                                    AUTOSAR modules."</p>
-                                                <button @click="generateReport(c)"
-                                                    class="text-[10px] font-black text-blue-400 hover:text-blue-300 uppercase tracking-widest flex items-center gap-2 mt-4">
-                                                    <i class="fas fa-file-pdf"></i> Generate Evaluation Dossier
-                                                </button>
-                                            </div>
-                                        </div>
-                                    </td>
-                                </tr>
-                            </tbody>
-                            </template>
-                            </tbody>
-                        </table>
+                        </template>
+                        <div x-show="!dbDiagResult?.submissions?.recent?.length"
+                            class="text-center text-slate-600 py-4 italic">No submissions found in database</div>
                     </div>
                 </div>
+                <div class="text-[10px] text-slate-600 text-center">DB Path: <span x-text="dbDiagResult?.dbPath"></span></div>
+            </div>
+            <div class="mt-4 flex gap-3">
+                <button @click="loadAllSubmissions(); showDbDiag = false"
+                    class="flex-1 bg-blue-600 hover:bg-blue-500 text-white py-3 rounded-xl font-bold text-xs transition-all">
+                    <i class="fas fa-sync-alt mr-2"></i> Load into Dashboard
+                </button>
+                <button @click="showDbDiag = false"
+                    class="bg-slate-800 hover:bg-slate-700 text-slate-400 px-6 py-3 rounded-xl font-bold text-xs transition-all">
+                    Close
+                </button>
             </div>
         </div>
-
     </div>
+
+    <!-- Off-Day Submission Warning Modal -->
+    <div x-show="showOffDayWarning" x-cloak x-transition
+        class="fixed inset-0 z-[350] bg-slate-950/90 backdrop-blur-md flex items-center justify-center p-6">
+        <div class="bg-slate-900 border border-amber-600/40 rounded-3xl p-10 max-w-md w-full text-center shadow-2xl shadow-amber-900/20">
+            <div class="w-16 h-16 bg-amber-500/20 rounded-full flex items-center justify-center mx-auto mb-5">
+                <i class="fas fa-calendar-times text-3xl text-amber-400"></i>
+            </div>
+            <h2 class="text-xl font-black text-white mb-2">Off-Schedule Submission</h2>
+            <p class="text-sm text-slate-400 mb-2">
+                Assignments are scheduled for <span class="text-amber-400 font-bold">Monday, Wednesday & Friday</span> only.
+            </p>
+            <p class="text-xs text-slate-500 mb-8">
+                Today is <span class="font-bold text-white" x-text="new Date().toLocaleDateString('en-IN', { weekday: 'long', month: 'short', day: 'numeric' })"></span>.
+                Your submission will be marked as <span class="text-amber-400 font-bold">off-schedule</span> in the report.
+            </p>
+            <div class="flex gap-3">
+                <button @click="showOffDayWarning = false"
+                    class="flex-1 bg-slate-800 hover:bg-slate-700 border border-slate-700 text-white py-3 rounded-xl font-bold text-sm transition-all">
+                    <i class="fas fa-times mr-2"></i> Cancel
+                </button>
+                <button @click="showOffDayWarning = false; showSubmitModal = true"
+                    class="flex-1 bg-amber-600 hover:bg-amber-500 text-white py-3 rounded-xl font-black text-sm transition-all">
+                    <i class="fas fa-paper-plane mr-2"></i> Submit Anyway
+                </button>
+            </div>
+        </div>
     </div>
 
     <!-- Submit Assignment Modal -->
@@ -1588,6 +1538,39 @@
         </div>
     </div>
 
+    <!-- ΓöÇΓöÇ 1-Minute Warning Toast ΓöÇΓöÇ -->
+    <div x-show="problemTimeWarning && user.role === 'candidate'" x-cloak x-transition
+        class="fixed top-6 left-1/2 -translate-x-1/2 z-[400] bg-amber-700 text-white px-8 py-4 rounded-2xl shadow-2xl flex items-center gap-3 text-sm font-bold">
+        <i class="fas fa-exclamation-triangle text-xl animate-pulse"></i>
+        <div>
+            <div class="font-black">ΓÅ▒ 1 Minute Remaining!</div>
+            <div class="text-[11px] text-amber-200 mt-0.5">Wrap up or your time will expire for this problem.</div>
+        </div>
+    </div>
+
+    <!-- ΓöÇΓöÇ Timer Expired Modal ΓöÇΓöÇ -->
+    <div x-show="problemTimerExpired && user.role === 'candidate'" x-cloak
+        class="fixed inset-0 z-[500] bg-slate-950/95 backdrop-blur-xl flex items-center justify-center p-6">
+        <div class="bg-slate-900 border border-red-700/50 rounded-3xl p-10 max-w-md w-full text-center shadow-2xl shadow-red-900/30">
+            <div class="w-20 h-20 bg-red-600/20 rounded-full flex items-center justify-center mx-auto mb-6">
+                <i class="fas fa-hourglass-end text-4xl text-red-400"></i>
+            </div>
+            <h2 class="text-2xl font-black text-white mb-2">Time's Up!</h2>
+            <p class="text-sm text-slate-400 mb-2">Your allocated time for this problem has expired.</p>
+            <p class="text-xs text-slate-500 mb-8">You can move to the next problem ΓÇö any banked time from previous problems has been used.</p>
+            <div class="flex gap-3">
+                <button @click="problemTimerExpired = false; startProblemTimer()"
+                    class="flex-1 bg-slate-800 hover:bg-slate-700 border border-slate-700 text-white py-3 rounded-xl font-bold text-sm transition-all">
+                    <i class="fas fa-redo mr-2"></i> Stay on Problem
+                </button>
+                <button @click="moveToNextProblem()"
+                    class="flex-1 bg-red-600 hover:bg-red-500 text-white py-3 rounded-xl font-black text-sm transition-all">
+                    <i class="fas fa-arrow-right mr-2"></i> Move to Next
+                </button>
+            </div>
+        </div>
+    </div>
+
     <!-- Success Toast -->
     <div x-show="successToast" x-transition:enter="transition ease-out duration-300"
         x-transition:enter-start="translate-y-4 opacity-0" x-transition:enter-end="translate-y-0 opacity-100"
@@ -1823,11 +1806,17 @@
                 rp2040LEDOn: false,
                 // Assignment Submission & Grading
                 showSubmitModal: false,
+                showOffDayWarning: false,
+                dbDiagLoading: false,
+                dbDiagResult: null,
+                showDbDiag: false,
+                weeklyReportSending: false,
                 submitDay: 1,
                 isSubmitting: false,
                 mySubmissions: [],
                 allSubmissions: [],
-                weeklyReportSending: false,
+                submissionsLoading: false,
+                submissionsError: '',
 
                 init() {
                     const urlParams = new URLSearchParams(window.location.search);
@@ -1920,6 +1909,8 @@
                                     }
 
                                     this._loadServerProgress();
+                                    if (data.user.role === "admin") { this.loadAllSubmissions(); this.loadCandidates(); }
+                                    if (data.user.role === "candidate") { this._restoreViolations(data.user.email); }
                                 } else {
                                     this.logout();
                                 }
@@ -1930,6 +1921,9 @@
 
                 // Admin Dashboard State
                 adminTab: 'overview',
+                topicSaving: null,
+                topicSaveMsg: '',
+                topicLoadError: false,
                 adminSearch: '',
                 adminSubFilter: 'all',
                 adminDayFilter: '',
@@ -1940,13 +1934,29 @@
                 isGrading: false,
                 // Anti-Cheat / Proctoring
                 violationCount: 0,
+                autoSaveStatus: '',        // '' | 'saving' | 'saved' | 'error'
+                autoSaveTimer: null,
+                _autoSaveTimer: null,
                 violationWarning: false,
                 criticalViolationWarning: false,
                 violationLog: [],
+                PROBLEM_LIMIT_SECS: 720,
+                timePool: 0,
+                problemStartTime: null,
+                problemElapsed: 0,
+                problemTimerInterval: null,
+                problemTimeWarning: false,
+                problemTimerExpired: false,
+                problemTimeLog: {},
+                totalTimeUsed: 0,
+
+
+
+
 
                 // Platform config (from /api/config)
                 topicFilter: [],   // empty = show all topics
-                topicVisibility: {}, // map of topic.name -> boolean
+                topicVisibility: window.__INITIAL_TOPIC_VISIBILITY__ || {}, // pre-injected by server
                 platformTitle: 'SkillLyncVirtualLab',
                 platformSubtitle: 'Industrial Grade Embedded & ECU Project Platform',
 
@@ -1964,15 +1974,11 @@
                     { id: 'STM32_HAL', name: 'STM32 HAL Programming', icon: 'fas fa-layer-group', difficulty: 'Intermediate', projectCount: 10, desc: 'STM32 HAL API programming ΓÇö HAL_GPIO, HAL_UART, HAL_TIM, HAL_ADC, I2C, SPI. HAL calls produce observable output for automated testing.' },
                     { id: 'FREERTOS_SIM', name: 'FreeRTOS Simulation', icon: 'fas fa-tasks', difficulty: 'Advanced', projectCount: 10, desc: 'Real FreeRTOS running on the server via its POSIX/Linux port ΓÇö tasks, queues, semaphores, mutexes, event groups & software timers.' },
                     { id: 'DEBUG_LAB', name: '≡ƒöº Bug Hunt: Debug Lab', icon: 'fas fa-bug text-red-400', difficulty: 'Advanced', projectCount: 10, desc: 'Real-world debugging challenges. Every project gives you broken code + a production incident report ΓÇö find and fix the root cause. This is what Day 1 at a company actually looks like.' },
-                    { id: 'VISTEON_C', name: 'Visteon C Programming Assignment Questions', icon: 'fas fa-building text-blue-500', difficulty: 'Advanced', projectCount: 100, desc: 'Exclusive intermediate to advanced C programming assignments covering structures, pointers, memory mapping, and more.' },
-                    { id: 'VISTEON_C_BASIC', name: 'Visteon C Programming: Basic ECU', icon: 'fas fa-car-side text-blue-400', difficulty: 'Basic', projectCount: 10, desc: 'Focus on core ECU control logic using basic C constructs: if-else, switch-case, and while loops. Simulates real vehicle control units.' }
+                    { id: 'VISTEON_C', name: 'Visteon C Programming Assignment Questions', icon: 'fas fa-building text-blue-500', difficulty: 'Advanced', projectCount: 20, desc: 'Exclusive intermediate to advanced C programming assignments covering structures, pointers, memory mapping, and more.' }
                 ],
 
 
-                candidates: [
-                    { name: 'Arjun Mehra', email: 'arjun.m@eng.com', code: 'SL-A8K1P2', completed: 18, hours: 42, strong: ['Embedded C', 'ADC'], weak: ['RTOS'], progress: 75, showDetails: false },
-                    { name: 'Priya Sharma', email: 'priya.s@tech.in', code: 'SL-Q2W9L0', completed: 25, hours: 58, strong: ['CPP17', 'SPI'], weak: ['DMA'], progress: 95, showDetails: false }
-                ],
+                candidates: [],
 
                 projects: [
                     // --- Linux System Programming (LINUX_SYS) ---
@@ -2006,118 +2012,268 @@
 
                     { id: 'LINUX-15', topicId: 'LINUX_SYS', title: 'Zombie Process Prevention', difficulty: 'Advanced', points: 150, statement: 'Create 3 child processes. Use SIGCHLD with SA_NOCLDWAIT flag (or signal(SIGCHLD, SIG_IGN)) to auto-reap children without calling wait(). After all children exit, parent prints "Reaped: 3" using a global counter incremented in a SIGCHLD handler (sigaction).', acceptance: ['SIGCHLD handler', 'No explicit wait() in main loop', 'All 3 reaped'], deliverables: ['main.c'], inputSpec: 'None', outputSpec: 'Reaped: 3', files: [{ name: 'main.c', content: '#include <stdio.h>\n#include <stdlib.h>\n#include <signal.h>\n#include <unistd.h>\n#include <sys/wait.h>\n\nvolatile int reaped = 0;\n\nvoid sigchld_handler(int sig) {\n    while (waitpid(-1, NULL, WNOHANG) > 0) reaped++;\n}\n\nint main() {\n    struct sigaction sa = { .sa_handler = sigchld_handler, .sa_flags = SA_RESTART };\n    sigaction(SIGCHLD, &sa, NULL);\n    for (int i = 0; i < 3; i++) {\n        if (fork() == 0) return 0; // child exits immediately\n    }\n    // TODO: wait until reaped == 3 (hint: use a small busy-wait loop)\n    printf("Reaped: %d\\n", reaped);\n    return 0;\n}' }], testCases: [{ name: 'Reap', stdin: '', expectedOutput: 'Reaped: 3' }] },
 
-                    // --- Visteon C Programming: Basic ECU (VISTEON_C_BASIC) ---
+                    // Assignment 1 ΓÇö Introduction to C & Program Structure
                     {
-                        id: 'ECU-01',
-                        topicId: 'VISTEON_C_BASIC',
-                        title: 'Q1: Basic ECU Control Logic',
-                        difficulty: 'Basic',
-                        points: 100,
-                        statement: 'Develop a simple Control Logic for a Vehicle.\n- Speed > 120 km/h: Print "Warning: Overspeed", else: "Speed Normal"\n- Temperature > 110: "Critical Overheat", > 95: "High Temperature", else: "Temperature Normal"\n- Gear (0: Neutral, 1-5: First-Fifth Gear, else: "Invalid Gear")\n\nConstraints: Use if-else, switch, and while(1). No arrays or functions.',
-                        acceptance: ['Correct speed logic', 'Correct temp logic', 'Switch for gear used'], deliverables: ['main.c'],
-                        inputSpec: 'Speed(int) Temp(int) Gear(int)',
-                        outputSpec: 'Overspeed status\nTemp status\nGear status',
-                        files: [{ name: 'main.c', content: '#include <stdio.h>\n\nint main() {\n    int s, t, g;\n    while(scanf("%d %d %d", &s, &t, &g) == 3) {\n        if(s > 120) printf("Warning: Overspeed\\n");\n        else printf("Speed Normal\\n");\n        if(t > 110) printf("Critical Overheat\\n");\n        else if(t > 95) printf("High Temperature\\n");\n        else printf("Temperature Normal\\n");\n        switch(g) {\n            case 0: printf("Neutral\\n"); break;\n            case 1: printf("First Gear\\n"); break;\n            case 2: printf("Second Gear\\n"); break;\n            case 3: printf("Third Gear\\n"); break;\n            case 4: printf("Fourth Gear\\n"); break;\n            case 5: printf("Fifth Gear\\n"); break;\n            default: printf("Invalid Gear\\n"); break;\n        }\n    }\n    return 0;\n}' }],
-                        testCases: [{ name: 'Normal', stdin: '100 80 4', expectedOutput: 'Speed Normal\nTemperature Normal\nFourth Gear' }, { name: 'Warning', stdin: '130 98 3', expectedOutput: 'Warning: Overspeed\nHigh Temperature\nThird Gear' }]
+                        id: 'VCA-01', topicId: 'VISTEON_C', title: 'A1-Q1: Vehicle Mode Controller',
+                        difficulty: 'Intermediate', points: 100,
+                        statement: 'Design and implement a Vehicle Mode Controller in C that manages the operating state of a vehicle using an enum and switch-case structure.\n\nThe controller must support:\n  0 ΓåÆ OFF\n  1 ΓåÆ ACC\n  2 ΓåÆ IGNITION_ON\n  3 ΓåÆ FAULT\n  Other ΓåÆ UNKNOWN_MODE\n\nRead mode value from stdin. Print the corresponding system response.',
+                        acceptance: ['enum used', 'switch-case for mode', 'All 4 modes handled', 'UNKNOWN_MODE for invalid'],
+                        deliverables: ['main.c'], inputSpec: 'Mode(int)', outputSpec: 'Mode string',
+                        files: [{ name: 'main.c', content: '#include <stdio.h>\n\ntypedef enum {\n    OFF = 0,\n    ACC,\n    IGNITION_ON,\n    FAULT\n} VehicleMode;\n\nint main() {\n    int m;\n    while (scanf("%d", &m) == 1) {\n        // TODO: switch on m and print mode string\n    }\n    return 0;\n}' }],
+                        testCases: [
+                            { name: 'OFF', stdin: '0', expectedOutput: 'OFF' },
+                            { name: 'ACC', stdin: '1', expectedOutput: 'ACC' },
+                            { name: 'IGNITION_ON', stdin: '2', expectedOutput: 'IGNITION_ON' },
+                            { name: 'FAULT', stdin: '3', expectedOutput: 'FAULT' },
+                            { name: 'Unknown', stdin: '9', expectedOutput: 'UNKNOWN_MODE' }
+                        ]
                     },
                     {
-                        id: 'ECU-02',
-                        topicId: 'VISTEON_C_BASIC',
-                        title: 'Q2: Fuel Level Monitor',
-                        difficulty: 'Basic', points: 80,
-                        statement: 'Monitor fuel level (0-100%).\n- < 10: "CRITICAL: Fuel Empty", < 25: "Warning: Low Fuel", < 50: "Fuel Below Half", else: "Fuel OK".',
-                        acceptance: ['Correct thresholds'], deliverables: ['main.c'],
-                        inputSpec: 'Fuel level(int)', outputSpec: 'Fuel status',
-                        files: [{ name: 'main.c', content: '#include <stdio.h>\n\nint main() {\n    int f;\n    while(scanf("%d", &f) == 1) {\n        if(f < 10) printf("CRITICAL: Fuel Empty\\n");\n        else if(f < 25) printf("Warning: Low Fuel\\n");\n        else if(f < 50) printf("Fuel Below Half\\n");\n        else printf("Fuel OK\\n");\n    }\n    return 0;\n}' }],
-                        testCases: [{ name: 'Critical', stdin: '8', expectedOutput: 'CRITICAL: Fuel Empty' }]
+                        id: 'VCA-02', topicId: 'VISTEON_C', title: 'A1-Q2: ECU Monitoring System',
+                        difficulty: 'Intermediate', points: 120,
+                        statement: 'Simulate a basic ECU monitoring system. Read: Speed (km/h), Engine Temperature (┬░C), Gear Position.\n\nSpeed check:\n  > 120 ΓåÆ "Warning: Overspeed"\n  else  ΓåÆ "Speed Normal"\n\nTemperature check:\n  > 110 ΓåÆ "Critical Overheat"\n  > 95  ΓåÆ "High Temperature"\n  else  ΓåÆ "Temperature Normal"\n\nGear (switch):\n  0 ΓåÆ "Neutral", 1 ΓåÆ "First Gear", 2 ΓåÆ "Second Gear"\n  3 ΓåÆ "Third Gear", 4 ΓåÆ "Fourth Gear", 5 ΓåÆ "Fifth Gear"\n  else ΓåÆ "Invalid Gear"\n\nRun inside while(1) loop, exit when scanf fails.',
+                        acceptance: ['All speed/temp/gear checks', 'switch for gear', 'while(1) loop'],
+                        deliverables: ['main.c'], inputSpec: 'Speed(int) Temp(int) Gear(int)', outputSpec: 'Speed status\nTemp status\nGear status',
+                        files: [{ name: 'main.c', content: '#include <stdio.h>\n\nint main() {\n    int s, t, g;\n    while (scanf("%d %d %d", &s, &t, &g) == 3) {\n        // TODO: Speed check\n        // TODO: Temp check\n        // TODO: Gear switch\n    }\n    return 0;\n}' }],
+                        testCases: [
+                            { name: 'Normal', stdin: '100 80 3', expectedOutput: 'Speed Normal\nTemperature Normal\nThird Gear' },
+                            { name: 'Warning', stdin: '130 98 2', expectedOutput: 'Warning: Overspeed\nHigh Temperature\nSecond Gear' },
+                            { name: 'Critical', stdin: '150 115 6', expectedOutput: 'Warning: Overspeed\nCritical Overheat\nInvalid Gear' }
+                        ]
                     },
+
+                    // Assignment 2 ΓÇö Data Types & Storage Classes
                     {
-                        id: 'ECU-03',
-                        topicId: 'VISTEON_C_BASIC',
-                        title: 'Q3: RPM Redline System',
-                        difficulty: 'Basic', points: 80,
-                        statement: 'Check RPM.\n- > 7000: "DANGER: Redline!", > 5500: "Warning: High RPM", > 3000: "RPM Normal", else: "Low RPM".',
-                        acceptance: ['RPM zones'], deliverables: ['main.c'],
-                        inputSpec: 'RPM(int)', outputSpec: 'RPM status',
-                        files: [{ name: 'main.c', content: '#include <stdio.h>\n\nint main() {\n    int r;\n    while(scanf("%d", &r) == 1) {\n        if(r > 7000) printf("DANGER: Redline!\\n");\n        else if(r > 5500) printf("Warning: High RPM\\n");\n        else if(r > 3000) printf("RPM Normal\\n");\n        else printf("Low RPM\\n");\n    }\n    return 0;\n}' }],
-                        testCases: [{ name: 'Redline', stdin: '7500', expectedOutput: 'DANGER: Redline!' }]
+                        id: 'VCA-03', topicId: 'VISTEON_C', title: 'A2-Q1: Storage Class Demonstrator',
+                        difficulty: 'Intermediate', points: 110,
+                        statement: 'Create a program demonstrating differences between auto, static, and extern variables across function calls.\n\nImplement a function counter() that:\n  - Has a static int that persists across calls\n  - Has an auto int that resets each call\n  - Prints both values each call\n\nCall counter() 3 times from main().\n\nExpected output per call:\n  Call N: auto=1 static=N',
+                        acceptance: ['static variable persists', 'auto resets each call', 'extern demonstrated', '3 calls shown'],
+                        deliverables: ['main.c'], inputSpec: 'None', outputSpec: 'Call 1: auto=1 static=1\nCall 2: auto=1 static=2\nCall 3: auto=1 static=3',
+                        files: [{ name: 'main.c', content: '#include <stdio.h>\n\nvoid counter() {\n    // TODO: auto int (resets) and static int (persists)\n    // print: "Call N: auto=X static=Y"\n}\n\nint main() {\n    counter();\n    counter();\n    counter();\n    return 0;\n}' }],
+                        testCases: [
+                            { name: 'Static persists', stdin: '', expectedOutput: 'Call 1: auto=1 static=1\nCall 2: auto=1 static=2\nCall 3: auto=1 static=3' }
+                        ]
                     },
                     {
-                        id: 'ECU-04',
-                        topicId: 'VISTEON_C_BASIC',
-                        title: 'Q4: Oil Pressure Monitor',
+                        id: 'VCA-04', topicId: 'VISTEON_C', title: 'A2-Q2: Expression Statements Demo',
                         difficulty: 'Basic', points: 80,
-                        statement: 'Monitor Oil pressure (PSI).\n- < 15: "CRITICAL: Engine Shutdown!", < 30: "Warning: Low", <= 80: "Normal", else: "High".',
-                        acceptance: ['Pressure zones'], deliverables: ['main.c'],
-                        inputSpec: 'PSI(int)', outputSpec: 'Pressure status',
-                        files: [{ name: 'main.c', content: '#include <stdio.h>\n\nint main() {\n    int p;\n    while(scanf("%d", &p) == 1) {\n        if(p < 15) printf("CRITICAL: Engine Shutdown!\\n");\n        else if(p < 30) printf("Warning: Low\\n");\n        else if(p <= 80) printf("Normal\\n");\n        else printf("High\\n");\n    }\n    return 0;\n}' }],
-                        testCases: [{ name: 'Critical', stdin: '10', expectedOutput: 'CRITICAL: Engine Shutdown!' }]
+                        statement: 'Develop a C program that demonstrates different statement types:\n  - Declaration statements\n  - Assignment statements\n  - Expression statements\n\nThe program must:\n  1. Declare two integers (a, b) and one float (c)\n  2. Assign: a=15, b=4, c=2.5\n  3. Compute and print: sum = a + b, product = a * b, result = a * c\n\nExpected output:\nSum: 19\nProduct: 60\nResult: 37.50',
+                        acceptance: ['Declaration, assignment, expression statements', 'Correct arithmetic', 'Correct output format'],
+                        deliverables: ['main.c'], inputSpec: 'None', outputSpec: 'Sum: 19\nProduct: 60\nResult: 37.50',
+                        files: [{ name: 'main.c', content: '#include <stdio.h>\n\nint main() {\n    // TODO: declare int a, b and float c\n    // TODO: assign values\n    // TODO: compute and print sum, product, result\n    return 0;\n}' }],
+                        testCases: [
+                            { name: 'Expressions', stdin: '', expectedOutput: 'Sum: 19\nProduct: 60\nResult: 37.50' }
+                        ]
                     },
+
+                    // Assignment 3 ΓÇö Functions & Pointers
                     {
-                        id: 'ECU-05',
-                        topicId: 'VISTEON_C_BASIC',
-                        title: 'Q5: Ignition Selector',
-                        difficulty: 'Basic', points: 70,
-                        statement: 'Key position (0: OFF, 1: ACC, 2: ON, 3: Start, else: Invalid). Use switch.',
-                        acceptance: ['Switch used'], deliverables: ['main.c'],
-                        inputSpec: 'Pos(int)', outputSpec: 'Status',
-                        files: [{ name: 'main.c', content: '#include <stdio.h>\n\nint main() {\n    int k;\n    while(scanf("%d", &k) == 1) {\n        switch(k) {\n            case 0: printf("OFF\\n"); break;\n            case 1: printf("ACC\\n"); break;\n            case 2: printf("ON\\n"); break;\n            case 3: printf("Start\\n"); break;\n            default: printf("Invalid\\n"); break;\n        }\n    }\n    return 0;\n}' }],
-                        testCases: [{ name: 'ACC', stdin: '1', expectedOutput: 'ACC' }]
+                        id: 'VCA-05', topicId: 'VISTEON_C', title: 'A3-Q1: Speed Update via Pointer',
+                        difficulty: 'Intermediate', points: 120,
+                        statement: 'Write a function update_speed(int *speed, int new_speed) that:\n  - Updates the speed variable through a pointer\n  - Returns 0 (success) if 0 <= new_speed <= 250\n  - Returns -1 (failure) if new_speed is out of range\n\nIn main(), read two speeds. Call update_speed for each. Print updated value or "Invalid Speed" based on return code.',
+                        acceptance: ['Pointer argument used', 'Return code 0/-1', 'Range validation', 'Correct output'],
+                        deliverables: ['main.c'], inputSpec: 'Speed1(int) Speed2(int)', outputSpec: 'Updated speed or "Invalid Speed" per line',
+                        files: [{ name: 'main.c', content: '#include <stdio.h>\n\nint update_speed(int *speed, int new_speed) {\n    // TODO: validate range 0-250, update via pointer, return 0 or -1\n    return 0;\n}\n\nint main() {\n    int s, v;\n    while (scanf("%d", &v) == 1) {\n        int ret = update_speed(&s, v);\n        if (ret == 0) printf("Speed: %d\\n", s);\n        else printf("Invalid Speed\\n");\n    }\n    return 0;\n}' }],
+                        testCases: [
+                            { name: 'Valid', stdin: '100', expectedOutput: 'Speed: 100' },
+                            { name: 'Invalid', stdin: '300', expectedOutput: 'Invalid Speed' },
+                            { name: 'Zero', stdin: '0', expectedOutput: 'Speed: 0' }
+                        ]
                     },
                     {
-                        id: 'ECU-06',
-                        topicId: 'VISTEON_C_BASIC',
-                        title: 'Q6: Climate Fan Control',
-                        difficulty: 'Basic', points: 90,
-                        statement: 'Cabin vs Setpoint. If Cabin > Setpoint + 3: "A/C High", Cabin > Setpoint: "A/C Low", < Setpoint - 3: "Heater High", < Setpoint: "Heater Low", else: "Fan Off".',
-                        acceptance: ['5 fan states'], deliverables: ['main.c'],
-                        inputSpec: 'Cabin(int) Set(int)', outputSpec: 'Fan status',
-                        files: [{ name: 'main.c', content: '#include <stdio.h>\n\nint main() {\n    int c, s;\n    while(scanf("%d %d", &c, &s) == 2) {\n        if(c > s + 3) printf("A/C High\\n");\n        else if(c > s) printf("A/C Low\\n");\n        else if(c < s - 3) printf("Heater High\\n");\n        else if(c < s) printf("Heater Low\\n");\n        else printf("Fan Off\\n");\n    }\n    return 0;\n}' }],
-                        testCases: [{ name: 'A/C High', stdin: '30 22', expectedOutput: 'A/C High' }]
+                        id: 'VCA-06', topicId: 'VISTEON_C', title: 'A3-Q2: Fault Flag Manager',
+                        difficulty: 'Intermediate', points: 130,
+                        statement: 'In embedded systems, faults are tracked using bit flags.\n\nImplement:\n  void set_fault(int *flags, int bit)   ΓÇö sets bit in flags\n  void clear_fault(int *flags, int bit) ΓÇö clears bit in flags\n\nFault bit map:\n  Bit 0 ΓåÆ OverSpeed\n  Bit 1 ΓåÆ OverTemperature\n  Bit 2 ΓåÆ SensorFailure\n\nRead N operations of the form:\n  SET <bit>   ΓåÆ set that bit\n  CLR <bit>   ΓåÆ clear that bit\n\nAfter all operations, print: "FaultRegister: 0x<hex>"',
+                        acceptance: ['Pointer access to flags', 'set_fault uses |= with bit shift', 'clear_fault uses &= ~', 'Correct hex output'],
+                        deliverables: ['main.c'], inputSpec: 'N\nN lines of: SET/CLR bit', outputSpec: 'FaultRegister: 0xXX',
+                        files: [{ name: 'main.c', content: '#include <stdio.h>\n\nvoid set_fault(int *flags, int bit) {\n    // TODO: set bit using pointer\n}\n\nvoid clear_fault(int *flags, int bit) {\n    // TODO: clear bit using pointer\n}\n\nint main() {\n    int flags = 0, n, bit;\n    char op[4];\n    scanf("%d", &n);\n    while (n--) {\n        scanf("%s %d", op, &bit);\n        if (op[0] == \'S\') set_fault(&flags, bit);\n        else clear_fault(&flags, bit);\n    }\n    printf("FaultRegister: 0x%02X\\n", flags);\n    return 0;\n}' }],
+                        testCases: [
+                            { name: 'Set all', stdin: '3\nSET 0\nSET 1\nSET 2', expectedOutput: 'FaultRegister: 0x07' },
+                            { name: 'Set then clear', stdin: '2\nSET 0\nCLR 0', expectedOutput: 'FaultRegister: 0x00' },
+                            { name: 'Partial', stdin: '2\nSET 0\nSET 2', expectedOutput: 'FaultRegister: 0x05' }
+                        ]
                     },
+
+                    // Assignment 4 ΓÇö Arrays & Strings
                     {
-                        id: 'ECU-07',
-                        topicId: 'VISTEON_C_BASIC',
-                        title: 'Q7: Brake Pressure Failure',
-                        difficulty: 'Basic', points: 80,
-                        statement: 'Monitor Brake Bar. < 5: "FAILURE", < 10: "Low", <= 20: "Normal", else: "Excessive".',
-                        acceptance: ['Brake status'], deliverables: ['main.c'],
-                        inputSpec: 'Bar(int)', outputSpec: 'Status',
-                        files: [{ name: 'main.c', content: '#include <stdio.h>\n\nint main() {\n    int b;\n    while(scanf("%d", &b) == 1) {\n        if(b < 5) printf("FAILURE\\n");\n        else if(b < 10) printf("Low\\n");\n        else if(b <= 20) printf("Normal\\n");\n        else printf("Excessive\\n");\n    }\n    return 0;\n}' }],
-                        testCases: [{ name: 'Failure', stdin: '3', expectedOutput: 'FAILURE' }]
+                        id: 'VCA-07', topicId: 'VISTEON_C', title: 'A4-Q1: Sensor Data Array',
+                        difficulty: 'Intermediate', points: 110,
+                        statement: 'Read N integer sensor readings into an array. Compute and print:\n  Min, Max, and Average (2 decimal places).\n\nExample:\nInput: 5\n       34 67 22 89 45\nOutput:\nMin: 22\nMax: 89\nAvg: 51.40',
+                        acceptance: ['Array used', 'Correct min/max/avg', 'Correct format'],
+                        deliverables: ['main.c'], inputSpec: 'N\nN integers', outputSpec: 'Min: X\nMax: X\nAvg: X.XX',
+                        files: [{ name: 'main.c', content: '#include <stdio.h>\n\nint main() {\n    int n;\n    scanf("%d", &n);\n    int arr[100];\n    for (int i = 0; i < n; i++) scanf("%d", &arr[i]);\n    // TODO: find min, max, avg and print\n    return 0;\n}' }],
+                        testCases: [
+                            { name: 'Basic', stdin: '5\n34 67 22 89 45', expectedOutput: 'Min: 22\nMax: 89\nAvg: 51.40' },
+                            { name: 'Single', stdin: '1\n10', expectedOutput: 'Min: 10\nMax: 10\nAvg: 10.00' }
+                        ]
                     },
                     {
-                        id: 'ECU-08',
-                        topicId: 'VISTEON_C_BASIC',
-                        title: 'Q8: Throttle Drive Mode',
-                        difficulty: 'Basic', points: 70,
-                        statement: 'Throttle % (0-100). 0: "Idle", <= 25: "Economy", <= 60: "Normal", <= 85: "Sport", else: "FULL".',
-                        acceptance: ['Mode selection'], deliverables: ['main.c'],
-                        inputSpec: 'Thr(int)', outputSpec: 'Mode',
-                        files: [{ name: 'main.c', content: '#include <stdio.h>\n\nint main() {\n    int th;\n    while(scanf("%d", &th) == 1) {\n        if(th == 0) printf("Idle\\n");\n        else if(th <= 25) printf("Economy\\n");\n        else if(th <= 60) printf("Normal\\n");\n        else if(th <= 85) printf("Sport\\n");\n        else printf("FULL\\n");\n    }\n    return 0;\n}' }],
-                        testCases: [{ name: 'Eco', stdin: '15', expectedOutput: 'Economy' }]
+                        id: 'VCA-08', topicId: 'VISTEON_C', title: 'A4-Q2: DTC Code Parser',
+                        difficulty: 'Intermediate', points: 120,
+                        statement: 'Read a DTC (Diagnostic Trouble Code) string of exactly 5 characters (e.g. "P0301").\n\nParse and print:\n  System: P=Powertrain, C=Chassis, B=Body, U=Network\n  Type:   digit[1] (0=Generic, 1=Manufacturer)\n  Code:   last 3 digits\n\nExample:\nInput: P0301\nOutput:\nSystem: Powertrain\nType: Generic\nCode: 301',
+                        acceptance: ['String/char array', 'First char parsed for system', 'Digit extraction', 'Correct output'],
+                        deliverables: ['main.c'], inputSpec: 'DTC string (5 chars)', outputSpec: 'System: X\nType: X\nCode: XXX',
+                        files: [{ name: 'main.c', content: '#include <stdio.h>\n\nint main() {\n    char dtc[6];\n    scanf("%5s", dtc);\n    // TODO: parse dtc[0] for system, dtc[1] for type, dtc[2..4] for code\n    return 0;\n}' }],
+                        testCases: [
+                            { name: 'Powertrain Generic', stdin: 'P0301', expectedOutput: 'System: Powertrain\nType: Generic\nCode: 301' },
+                            { name: 'Chassis Mfr', stdin: 'C1234', expectedOutput: 'System: Chassis\nType: Manufacturer\nCode: 234' },
+                            { name: 'Body', stdin: 'B0512', expectedOutput: 'System: Body\nType: Generic\nCode: 512' }
+                        ]
                     },
+
+                    // Assignment 5 ΓÇö Structures & Unions
+                    {
+                        id: 'VCA-09', topicId: 'VISTEON_C', title: 'A5-Q1: Vehicle Struct',
+                        difficulty: 'Intermediate', points: 120,
+                        statement: 'Define a struct Vehicle with fields:\n  char model[20], int year, float mileage, int is_electric\n\nRead N vehicles. Print details and flag electric ones.\n\nOutput per vehicle:\n  Model: <model>, Year: <year>, Mileage: <mileage>.XX km [ELECTRIC] or [FUEL]',
+                        acceptance: ['struct used', 'All fields read', 'ELECTRIC/FUEL flag', 'Correct format'],
+                        deliverables: ['main.c'], inputSpec: 'N\nN lines: model year mileage is_electric', outputSpec: 'One line per vehicle',
+                        files: [{ name: 'main.c', content: '#include <stdio.h>\n\ntypedef struct {\n    char model[20];\n    int year;\n    float mileage;\n    int is_electric;\n} Vehicle;\n\nint main() {\n    int n;\n    scanf("%d", &n);\n    Vehicle v[10];\n    for (int i = 0; i < n; i++) {\n        scanf("%s %d %f %d", v[i].model, &v[i].year, &v[i].mileage, &v[i].is_electric);\n    }\n    // TODO: print each vehicle\n    return 0;\n}' }],
+                        testCases: [
+                            { name: 'Electric', stdin: '1\nTesla 2023 15000.00 1', expectedOutput: 'Model: Tesla, Year: 2023, Mileage: 15000.00 km [ELECTRIC]' },
+                            { name: 'Fuel', stdin: '1\nFord 2020 45000.00 0', expectedOutput: 'Model: Ford, Year: 2020, Mileage: 45000.00 km [FUEL]' }
+                        ]
+                    },
+                    {
+                        id: 'VCA-10', topicId: 'VISTEON_C', title: 'A5-Q2: CAN Frame Union',
+                        difficulty: 'Advanced', points: 150,
+                        statement: 'In CAN bus, a 4-byte data frame can be viewed as individual bytes or as a 32-bit word.\n\nDefine a union CANFrame:\n  uint32_t raw\n  uint8_t  bytes[4]\n\nRead a 32-bit hex value (e.g. 0xDEADBEEF). Store in raw. Print each byte separately:\n  Byte0: 0xEF\n  Byte1: 0xBE\n  Byte2: 0xAD\n  Byte3: 0xDE\n(little-endian order)',
+                        acceptance: ['union used', 'uint32_t raw + uint8_t bytes[4]', 'Correct little-endian output'],
+                        deliverables: ['main.c'], inputSpec: 'Hex value (e.g. DEADBEEF)', outputSpec: 'Byte0..Byte3 in hex',
+                        files: [{ name: 'main.c', content: '#include <stdio.h>\n#include <stdint.h>\n\ntypedef union {\n    uint32_t raw;\n    uint8_t  bytes[4];\n} CANFrame;\n\nint main() {\n    CANFrame f;\n    scanf("%X", &f.raw);\n    // TODO: print bytes[0..3]\n    return 0;\n}' }],
+                        testCases: [
+                            { name: 'DEADBEEF', stdin: 'DEADBEEF', expectedOutput: 'Byte0: 0xEF\nByte1: 0xBE\nByte2: 0xAD\nByte3: 0xDE' },
+                            { name: 'CAFEBABE', stdin: 'CAFEBABE', expectedOutput: 'Byte0: 0xBE\nByte1: 0xBA\nByte2: 0xFE\nByte3: 0xCA' }
+                        ]
+                    },
+
+                    // Assignment 6 ΓÇö Memory Management
                     {
-                        id: 'ECU-09',
-                        topicId: 'VISTEON_C_BASIC',
-                        title: 'Q9: Wiper Control',
-                        difficulty: 'Basic', points: 70,
-                        statement: 'Rain(0-100) and Sw(0/1). If Sw is ON: rain > 70: "Fast", > 40: "Medium", > 10: "Slow", else "Off". If Sw is OFF: "Off".',
-                        acceptance: ['Switch handled'], deliverables: ['main.c'],
-                        inputSpec: 'Rain(int) Sw(int)', outputSpec: 'Wiper status',
-                        files: [{ name: 'main.c', content: '#include <stdio.h>\n\nint main() {\n    int r, s;\n    while(scanf("%d %d", &r, &s) == 2) {\n        if(s == 1) {\n            if(r > 70) printf("Fast\\n");\n            else if(r > 40) printf("Medium\\n");\n            else if(r > 10) printf("Slow\\n");\n            else printf("Off\\n");\n        } else {\n            printf("Off\\n");\n        }\n    }\n    return 0;\n}' }],
-                        testCases: [{ name: 'Fast', stdin: '80 1', expectedOutput: 'Fast' }]
+                        id: 'VCA-11', topicId: 'VISTEON_C', title: 'A6-Q1: Dynamic Sensor Buffer',
+                        difficulty: 'Advanced', points: 140,
+                        statement: 'Dynamically allocate an array of N integers using malloc().\nRead N values. Compute sum and average. Free the memory.\n\nOutput:\nSum: <sum>\nAvg: <avg>.XX\nMemory freed.',
+                        acceptance: ['malloc used', 'free called', 'Correct sum/avg', '"Memory freed." printed'],
+                        deliverables: ['main.c'], inputSpec: 'N\nN integers', outputSpec: 'Sum: X\nAvg: X.XX\nMemory freed.',
+                        files: [{ name: 'main.c', content: '#include <stdio.h>\n#include <stdlib.h>\n\nint main() {\n    int n;\n    scanf("%d", &n);\n    int *buf = (int*)malloc(n * sizeof(int));\n    // TODO: read values, compute sum/avg, free, print\n    return 0;\n}' }],
+                        testCases: [
+                            { name: 'Basic', stdin: '4\n10 20 30 40', expectedOutput: 'Sum: 100\nAvg: 25.00\nMemory freed.' },
+                            { name: 'Single', stdin: '1\n7', expectedOutput: 'Sum: 7\nAvg: 7.00\nMemory freed.' }
+                        ]
                     },
                     {
-                        id: 'ECU-10',
-                        topicId: 'VISTEON_C_BASIC',
-                        title: 'Q10: Airbag Decision',
+                        id: 'VCA-12', topicId: 'VISTEON_C', title: 'A6-Q2: Linked List ECU Log',
+                        difficulty: 'Advanced', points: 160,
+                        statement: 'Implement a singly linked list to store ECU event logs.\nEach node: int event_id, char message[30], next pointer.\n\nRead N events then print all in insertion order.\nFree all nodes at the end.\n\nOutput per node:\nEvent <id>: <message>',
+                        acceptance: ['struct with next pointer', 'malloc per node', 'Traversal prints correctly', 'All nodes freed'],
+                        deliverables: ['main.c'], inputSpec: 'N\nN lines: id message', outputSpec: 'N lines: Event X: message',
+                        files: [{ name: 'main.c', content: '#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n\ntypedef struct Node {\n    int event_id;\n    char message[30];\n    struct Node *next;\n} Node;\n\nint main() {\n    int n;\n    scanf("%d", &n);\n    Node *head = NULL, *tail = NULL;\n    for (int i = 0; i < n; i++) {\n        Node *node = (Node*)malloc(sizeof(Node));\n        scanf("%d %s", &node->event_id, node->message);\n        node->next = NULL;\n        // TODO: append to list\n    }\n    // TODO: traverse and print, then free all\n    return 0;\n}' }],
+                        testCases: [
+                            { name: 'Two events', stdin: '2\n101 OverSpeed\n202 SensorFail', expectedOutput: 'Event 101: OverSpeed\nEvent 202: SensorFail' }
+                        ]
+                    },
+
+                    // Assignment 7 ΓÇö File I/O
+                    {
+                        id: 'VCA-13', topicId: 'VISTEON_C', title: 'A7-Q1: ECU Log Writer',
+                        difficulty: 'Advanced', points: 130,
+                        statement: 'Write N log entries to a file "ecu_log.txt", then read them back and print.\n\nEach entry format: "LOG[N]: <message>"\n\nAfter writing and reading back, print each line as-is.\nFinally print: "Log complete."',
+                        acceptance: ['fopen/fclose used', 'fprintf to file', 'fgets to read back', '"Log complete." at end'],
+                        deliverables: ['main.c'], inputSpec: 'N\nN message strings', outputSpec: 'N log lines then "Log complete."',
+                        files: [{ name: 'main.c', content: '#include <stdio.h>\n\nint main() {\n    int n; char msg[50];\n    scanf("%d", &n);\n    FILE *f = fopen("ecu_log.txt", "w");\n    for (int i = 1; i <= n; i++) {\n        scanf("%s", msg);\n        fprintf(f, "LOG[%d]: %s\\n", i, msg);\n    }\n    fclose(f);\n    // TODO: reopen for read, fgets each line and print\n    printf("Log complete.\\n");\n    return 0;\n}' }],
+                        testCases: [
+                            { name: 'Two logs', stdin: '2\nStart Stop', expectedOutput: 'LOG[1]: Start\nLOG[2]: Stop\nLog complete.' }
+                        ]
+                    },
+                    {
+                        id: 'VCA-14', topicId: 'VISTEON_C', title: 'A7-Q2: Config File Parser',
+                        difficulty: 'Advanced', points: 140,
+                        statement: 'Parse a config-style input of KEY=VALUE pairs (N lines). Store them and look up values by key.\n\nAfter reading N pairs, read M query keys. For each, print the value or "NOT_FOUND".',
+                        acceptance: ['String split on "="', 'Key lookup', '"NOT_FOUND" for missing', 'Correct output'],
+                        deliverables: ['main.c'], inputSpec: 'N\nN lines: KEY=VALUE\nM\nM keys', outputSpec: 'Value or NOT_FOUND per query',
+                        files: [{ name: 'main.c', content: '#include <stdio.h>\n#include <string.h>\n\nint main() {\n    int n, m;\n    char keys[20][32], vals[20][32], line[64], qkey[32];\n    scanf("%d", &n);\n    for (int i = 0; i < n; i++) {\n        scanf("%s", line);\n        char *eq = strchr(line, \'=\');\n        // TODO: split on \'=\' into keys[i] and vals[i]\n    }\n    scanf("%d", &m);\n    while (m--) {\n        scanf("%s", qkey);\n        // TODO: find and print value or NOT_FOUND\n    }\n    return 0;\n}' }],
+                        testCases: [
+                            { name: 'Found', stdin: '2\nSPEED=120\nTEMP=95\n1\nSPEED', expectedOutput: '120' },
+                            { name: 'Not found', stdin: '1\nSPEED=100\n1\nFUEL', expectedOutput: 'NOT_FOUND' }
+                        ]
+                    },
+
+                    // Assignment 8 ΓÇö Bitwise Operations
+                    {
+                        id: 'VCA-15', topicId: 'VISTEON_C', title: 'A8-Q1: Register Bit Manipulator',
+                        difficulty: 'Advanced', points: 140,
+                        statement: 'Simulate MCU register bit operations on an 8-bit register (uint8_t).\n\nStarting value: 0x00\nRead N operations:\n  SET <bit>    ΓåÆ set bit (0-7)\n  CLR <bit>    ΓåÆ clear bit\n  TOG <bit>    ΓåÆ toggle bit\n  GET <bit>    ΓåÆ print bit value (0 or 1)\n\nPrint register state as "REG: 0xXX" at the end.',
+                        acceptance: ['All 4 operations', 'Bitwise operators', 'GET prints 0/1', 'Final REG output'],
+                        deliverables: ['main.c'], inputSpec: 'N\nN operations', outputSpec: 'GET outputs then "REG: 0xXX"',
+                        files: [{ name: 'main.c', content: '#include <stdio.h>\n#include <stdint.h>\n#include <string.h>\n\nint main() {\n    uint8_t reg = 0x00;\n    int n, bit;\n    char op[4];\n    scanf("%d", &n);\n    while (n--) {\n        scanf("%s %d", op, &bit);\n        if (!strcmp(op,"SET"))      reg |=  (1 << bit);\n        else if (!strcmp(op,"CLR")) reg &= ~(1 << bit);\n        else if (!strcmp(op,"TOG")) reg ^=  (1 << bit);\n        else if (!strcmp(op,"GET")) printf("%d\\n", (reg >> bit) & 1);\n    }\n    printf("REG: 0x%02X\\n", reg);\n    return 0;\n}' }],
+                        testCases: [
+                            { name: 'Set and get', stdin: '3\nSET 0\nSET 3\nGET 0', expectedOutput: '1\nREG: 0x09' },
+                            { name: 'Toggle', stdin: '3\nSET 1\nTOG 1\nGET 1', expectedOutput: '0\nREG: 0x00' }
+                        ]
+                    },
+                    {
+                        id: 'VCA-16', topicId: 'VISTEON_C', title: 'A8-Q2: CAN ID Bit Extractor',
+                        difficulty: 'Advanced', points: 150,
+                        statement: 'A CAN 2.0B extended frame ID is 29 bits. Given a 32-bit hex value, extract:\n  Priority  : bits 28-26 (3 bits)\n  Reserved  : bit 25 (1 bit)\n  Data Page : bit 24 (1 bit)\n  PF        : bits 23-16 (8 bits)\n  PS        : bits 15-8  (8 bits)\n  SA        : bits 7-0   (8 bits)\n\nPrint each field in decimal.',
+                        acceptance: ['Correct bit masks', 'Correct shifts', 'All 6 fields printed'],
+                        deliverables: ['main.c'], inputSpec: 'Hex ID (32-bit)', outputSpec: 'Priority: X\nReserved: X\nDataPage: X\nPF: X\nPS: X\nSA: X',
+                        files: [{ name: 'main.c', content: '#include <stdio.h>\n#include <stdint.h>\n\nint main() {\n    uint32_t id;\n    scanf("%X", &id);\n    // TODO: extract each field using masks and shifts\n    return 0;\n}' }],
+                        testCases: [
+                            { name: 'Basic', stdin: '18FEF100', expectedOutput: 'Priority: 6\nReserved: 0\nDataPage: 0\nPF: 254\nPS: 241\nSA: 0' }
+                        ]
+                    },
+
+                    // Assignment 9 ΓÇö Preprocessor & Macros
+                    {
+                        id: 'VCA-17', topicId: 'VISTEON_C', title: 'A9-Q1: ECU Safety Macros',
+                        difficulty: 'Intermediate', points: 110,
+                        statement: 'Define macros for ECU safety thresholds and operations:\n  #define MAX_SPEED    120\n  #define MAX_TEMP     110\n  #define IS_OVERSPEED(s)  ((s) > MAX_SPEED)\n  #define IS_OVERHEAT(t)   ((t) > MAX_TEMP)\n  #define CLAMP(x,lo,hi)   ((x)<(lo)?(lo):((x)>(hi)?(hi):(x)))\n\nRead speed and temp. Print warnings using macros. Then clamp speed to 0-120 and print "Clamped: <val>".',
+                        acceptance: ['All macros defined', 'IS_OVERSPEED/IS_OVERHEAT used', 'CLAMP applied', 'Correct output'],
+                        deliverables: ['main.c'], inputSpec: 'Speed(int) Temp(int)', outputSpec: 'Warnings then "Clamped: X"',
+                        files: [{ name: 'main.c', content: '#include <stdio.h>\n\n// TODO: define MAX_SPEED, MAX_TEMP, IS_OVERSPEED, IS_OVERHEAT, CLAMP\n\nint main() {\n    int s, t;\n    scanf("%d %d", &s, &t);\n    if (IS_OVERSPEED(s)) printf("Warning: Overspeed\\n");\n    if (IS_OVERHEAT(t))  printf("Warning: Overheat\\n");\n    printf("Clamped: %d\\n", CLAMP(s, 0, 120));\n    return 0;\n}' }],
+                        testCases: [
+                            { name: 'Both warnings', stdin: '150 115', expectedOutput: 'Warning: Overspeed\nWarning: Overheat\nClamped: 120' },
+                            { name: 'Normal', stdin: '80 90', expectedOutput: 'Clamped: 80' },
+                            { name: 'Negative clamp', stdin: '-10 50', expectedOutput: 'Clamped: 0' }
+                        ]
+                    },
+                    {
+                        id: 'VCA-18', topicId: 'VISTEON_C', title: 'A9-Q2: Conditional Compilation',
                         difficulty: 'Intermediate', points: 100,
-                        statement: 'Impact(G) and Belt(0/1). If Impact > 30 or (Impact > 20 and Belt == 0): "DEPLOYED", else: "Normal".',
-                        acceptance: ['Deployment logic'], deliverables: ['main.c'],
-                        inputSpec: 'Imp(int) Belt(int)', outputSpec: 'Airbag status',
-                        files: [{ name: 'main.c', content: '#include <stdio.h>\n\nint main() {\n    int i, b;\n    while(scanf("%d %d", &i, &b) == 2) {\n        if(i > 30 || (i > 20 && b == 0)) printf("DEPLOYED\\n");\n        else printf("Normal\\n");\n    }\n    return 0;\n}' }],
-                        testCases: [{ name: 'Deploy', stdin: '35 1', expectedOutput: 'DEPLOYED' }]
+                        statement: 'Use conditional compilation to select between DEBUG and RELEASE build behaviour.\n\nIf DEBUG is defined:\n  Print "DEBUG: Speed=<s> Temp=<t>"\n  Then normal processing\nIf not DEBUG (RELEASE):\n  Only print results, no debug info\n\nFor this exercise, define DEBUG via #define in your code.\n\nRead Speed and Temp. Print the debug line then:\n  Speed status and Temp status (same logic as A1-Q2).',
+                        acceptance: ['#define DEBUG', '#ifdef DEBUG used', 'Correct conditional output'],
+                        deliverables: ['main.c'], inputSpec: 'Speed(int) Temp(int)', outputSpec: 'DEBUG line + status lines',
+                        files: [{ name: 'main.c', content: '#include <stdio.h>\n\n#define DEBUG\n\nint main() {\n    int s, t;\n    scanf("%d %d", &s, &t);\n    #ifdef DEBUG\n    printf("DEBUG: Speed=%d Temp=%d\\n", s, t);\n    #endif\n    // TODO: print speed and temp status\n    return 0;\n}' }],
+                        testCases: [
+                            { name: 'Debug mode', stdin: '100 80', expectedOutput: 'DEBUG: Speed=100 Temp=80\nSpeed Normal\nTemperature Normal' },
+                            { name: 'Warning', stdin: '130 112', expectedOutput: 'DEBUG: Speed=130 Temp=112\nWarning: Overspeed\nCritical Overheat' }
+                        ]
+                    },
+
+                    // Assignment 10 ΓÇö Interrupt & Timer Simulation
+                    {
+                        id: 'VCA-19', topicId: 'VISTEON_C', title: 'A10-Q1: Watchdog Timer Simulation',
+                        difficulty: 'Advanced', points: 150,
+                        statement: 'Simulate a software Watchdog Timer.\n\nLogic:\n  - WDT timeout = 5 ticks\n  - Read N events: "KICK" resets the counter, "TICK" increments it\n  - If counter reaches 5 without a KICK ΓåÆ print "WDT RESET" and stop\n  - If all events processed without timeout ΓåÆ print "System OK"\n\nPrint counter value after each event: "Tick: <count>"',
+                        acceptance: ['Counter resets on KICK', 'Increments on TICK', 'WDT RESET at 5', '"System OK" if no timeout'],
+                        deliverables: ['main.c'], inputSpec: 'N\nN events (TICK/KICK)', outputSpec: 'Tick lines then WDT RESET or System OK',
+                        files: [{ name: 'main.c', content: '#include <stdio.h>\n#include <string.h>\n\n#define WDT_TIMEOUT 5\n\nint main() {\n    int n, counter = 0;\n    char evt[5];\n    scanf("%d", &n);\n    for (int i = 0; i < n; i++) {\n        scanf("%s", evt);\n        if (!strcmp(evt, "KICK")) counter = 0;\n        else counter++;\n        printf("Tick: %d\\n", counter);\n        if (counter >= WDT_TIMEOUT) {\n            printf("WDT RESET\\n");\n            return 0;\n        }\n    }\n    printf("System OK\\n");\n    return 0;\n}' }],
+                        testCases: [
+                            { name: 'Timeout', stdin: '6\nTICK\nTICK\nTICK\nTICK\nTICK\nTICK', expectedOutput: 'Tick: 1\nTick: 2\nTick: 3\nTick: 4\nTick: 5\nWDT RESET' },
+                            { name: 'Kick saves', stdin: '4\nTICK\nTICK\nKICK\nTICK', expectedOutput: 'Tick: 1\nTick: 2\nTick: 0\nTick: 1\nSystem OK' }
+                        ]
+                    },
+                    {
+                        id: 'VCA-20', topicId: 'VISTEON_C', title: 'A10-Q2: PWM Duty Cycle Calculator',
+                        difficulty: 'Advanced', points: 150,
+                        statement: 'Simulate PWM signal parameters for motor control.\n\nGiven:\n  Frequency (Hz) and Duty Cycle (%)\n\nCalculate and print:\n  Period    = 1,000,000 / freq  (microseconds)\n  ON Time   = Period * duty / 100\n  OFF Time  = Period - ON Time\n\nAll values printed as integers (round down).\n\nExample:\nInput: 1000 75\nOutput:\nPeriod: 1000 us\nON: 750 us\nOFF: 250 us',
+                        acceptance: ['Correct period formula', 'Correct ON/OFF times', 'Integer arithmetic', 'Correct format'],
+                        deliverables: ['main.c'], inputSpec: 'Freq(int) Duty(int)', outputSpec: 'Period: X us\nON: X us\nOFF: X us',
+                        files: [{ name: 'main.c', content: '#include <stdio.h>\n\nint main() {\n    int freq, duty;\n    scanf("%d %d", &freq, &duty);\n    // TODO: compute period, on_time, off_time\n    return 0;\n}' }],
+                        testCases: [
+                            { name: '1kHz 75%', stdin: '1000 75', expectedOutput: 'Period: 1000 us\nON: 750 us\nOFF: 250 us' },
+                            { name: '500Hz 50%', stdin: '500 50', expectedOutput: 'Period: 2000 us\nON: 1000 us\nOFF: 1000 us' },
+                            { name: '2kHz 25%', stdin: '2000 25', expectedOutput: 'Period: 500 us\nON: 125 us\nOFF: 375 us' }
+                        ]
                     },
 
                     { id: 'LINUX-16', topicId: 'LINUX_SYS', title: 'Memory-Mapped File', difficulty: 'Advanced', points: 250, statement: 'Use mmap() to map /tmp/vlab_mmap_test.txt into memory. Write "MMAP_HELLO" to the file via the mapping. Then read it back with a second mmap call and print the content.', acceptance: ['mmap/munmap used', 'ftruncate to set size', 'Content matches'], deliverables: ['main.c'], inputSpec: 'None', outputSpec: 'MMAP_HELLO', files: [{ name: 'main.c', content: '#include <stdio.h>\n#include <stdlib.h>\n#include <string.h>\n#include <fcntl.h>\n#include <sys/mman.h>\n#include <unistd.h>\n\n#define FILE_PATH "/tmp/vlab_mmap_test.txt"\n#define DATA "MMAP_HELLO"\n\nint main() {\n    // TODO: open file, ftruncate to strlen(DATA)+1 bytes\n    // TODO: mmap with PROT_WRITE, write DATA into mapping, munmap\n    // TODO: open again, mmap with PROT_READ, printf the content, munmap\n    // Cleanup: unlink(FILE_PATH)\n    return 0;\n}' }], testCases: [{ name: 'MMAP', stdin: '', expectedOutput: 'MMAP_HELLO' }] },
@@ -2499,8 +2655,7 @@
                     this.$watch('adminTab', () => this.saveSession());
                     this.$watch('activeFileIdx', () => this.saveSession());
 
-                    // Proctoring / Anti-Cheat
-                    /* TEMPORARILY DISABLED FOR TESTING
+                    // ΓöÇΓöÇ Proctoring / Anti-Cheat (ACTIVE) ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
                     document.addEventListener('contextmenu', (e) => {
                         if (this.isLoggedIn && this.user.role === 'candidate') {
                             e.preventDefault();
@@ -2512,19 +2667,31 @@
                             this.registerViolation('Tab switch / window unfocused');
                         }
                     });
+                    window.addEventListener('blur', () => {
+                        if (this.isLoggedIn && this.user.role === 'candidate') {
+                            this.registerViolation('Window focus lost (possible tab switch)');
+                        }
+                    });
                     document.addEventListener('keydown', (e) => {
                         if (!this.isLoggedIn || this.user.role !== 'candidate') return;
-                        if (e.key === 'F12' || (e.ctrlKey && e.shiftKey && e.key === 'I') || (e.ctrlKey && e.key === 'u')) {
+                        // Block DevTools
+                        if (e.key === 'F12' ||
+                            (e.ctrlKey && e.shiftKey && (e.key === 'I' || e.key === 'i' || e.key === 'J' || e.key === 'j' || e.key === 'C' || e.key === 'c')) ||
+                            (e.ctrlKey && (e.key === 'u' || e.key === 'U'))) {
                             e.preventDefault();
-                            this.registerViolation('DevTools/Source access shortcut blocked');
+                            this.registerViolation('DevTools/Source access attempt blocked');
                         }
                         // Block copy, cut, paste
                         if (e.ctrlKey && (e.key === 'c' || e.key === 'C' || e.key === 'x' || e.key === 'X' || e.key === 'v' || e.key === 'V')) {
                             e.preventDefault();
                             this.registerViolation('Copy/Paste/Cut attempt blocked');
                         }
+                        // Block save, print, select-all
+                        if (e.ctrlKey && (e.key === 's' || e.key === 'S' || e.key === 'p' || e.key === 'P' || e.key === 'a' || e.key === 'A')) {
+                            e.preventDefault();
+                            this.registerViolation('Save/Print/Select-All attempt blocked');
+                        }
                     });
-                    // Also block copy/paste via clipboard events
                     document.addEventListener('copy', (e) => {
                         if (this.isLoggedIn && this.user.role === 'candidate') {
                             e.preventDefault();
@@ -2543,7 +2710,19 @@
                             this.registerViolation('Paste attempt blocked');
                         }
                     });
-                    */
+                    // Block drag-to-copy (dragging selected text out of editor)
+                    document.addEventListener('dragstart', (e) => {
+                        if (this.isLoggedIn && this.user.role === 'candidate') {
+                            e.preventDefault();
+                            this.registerViolation('Drag/drop attempt blocked');
+                        }
+                    });
+                    // Block print via browser menu (Ctrl+P already caught above, this catches menu print)
+                    window.addEventListener('beforeprint', (e) => {
+                        if (this.isLoggedIn && this.user.role === 'candidate') {
+                            this.registerViolation('Print attempt detected');
+                        }
+                    });
                 },
 
                 _getToken() { return localStorage.getItem('vlab_token'); },
@@ -2553,23 +2732,58 @@
                     return token ? { 'Content-Type': 'application/json', 'Authorization': 'Bearer ' + token } : { 'Content-Type': 'application/json' };
                 },
 
+                _restoreViolations(email) {
+                    try {
+                        const stored = localStorage.getItem('vlab_violations_' + email);
+                        if (stored) {
+                            const { count, log } = JSON.parse(stored);
+                            // Only restore if more violations than current (don't overwrite a fresh session)
+                            if (count > this.violationCount) {
+                                this.violationCount = count || 0;
+                                this.violationLog = log || [];
+                            }
+                        }
+                    } catch(e) {}
+                },
+
                 async _loadServerProgress() {
                     try {
                         const r = await fetch('/api/progress', { headers: this._authHeaders() });
                         if (r.ok) {
                             const data = await r.json();
-                            this.solvedProjectIds = data.solvedProjectIds || [];
+                            const serverIds = data.solvedProjectIds || [];
+                            // Merge: union of server + localStorage so neither source loses data
+                            const localIds = JSON.parse(localStorage.getItem('solvedProjectIds') || '[]');
+                            const merged = [...new Set([...serverIds, ...localIds])];
+                            this.solvedProjectIds = merged;
+                            // Persist merged back to localStorage as source of truth
+                            localStorage.setItem('solvedProjectIds', JSON.stringify(merged));
+                            // If localStorage had IDs the server didn't ΓÇö sync them up to server
+                            const missing = localIds.filter(id => !serverIds.includes(id));
+                            missing.forEach(id => {
+                                fetch('/api/progress/save', {
+                                    method: 'POST',
+                                    headers: this._authHeaders(),
+                                    body: JSON.stringify({ projectId: id, files: [], solved: true })
+                                }).catch(() => {});
+                            });
                         }
-                    } catch { } // Fallback to localStorage silently
+                    } catch {
+                        // Server unreachable ΓÇö fall back to localStorage
+                        const localIds = JSON.parse(localStorage.getItem('solvedProjectIds') || '[]');
+                        if (localIds.length > 0) this.solvedProjectIds = localIds;
+                    }
                 },
 
                 _saveProgressToServer(projectId, files, solved) {
                     // Fire-and-forget: save progress in background without blocking UI
                     if (!this.isLoggedIn || !this._getToken()) return;
+                    // Never downgrade a solved problem ΓÇö if it's already in solvedProjectIds keep it solved
+                    const isSolved = solved || this.solvedProjectIds.includes(projectId);
                     fetch('/api/progress/save', {
                         method: 'POST',
                         headers: this._authHeaders(),
-                        body: JSON.stringify({ projectId, files, solved })
+                        body: JSON.stringify({ projectId, files, solved: isSolved })
                     }).catch(() => { }); // Silently ignore network errors
                 },
 
@@ -2601,6 +2815,12 @@
                 },
 
                 logout() {
+                    // Preserve session state so user resumes where they left off
+                    const session = localStorage.getItem('virtual_lab_session');
+                    const email = this.user?.email;
+                    if (session && email) {
+                        localStorage.setItem('vlab_session_' + email, session);
+                    }
                     localStorage.removeItem('vlab_token');
                     localStorage.removeItem('virtual_lab_session');
                     this.isLoggedIn = false;
@@ -2620,6 +2840,11 @@
 
                 async generateCode() {
                     if (this.authLoading) return;
+                    // ΓöÇΓöÇ Visteon-only enforcement ΓöÇΓöÇ
+                    if (!window.isAllowedUser(this.user.email)) {
+                        alert('Access Restricted: This platform is only available to Visteon employees. Please use your @visteon.com email address.');
+                        return;
+                    }
                     this.authLoading = true;
                     try {
                         const r = await fetch('/api/auth/register', {
@@ -2658,9 +2883,14 @@
                             this.user = { name: data.user.name, email: data.user.email, role: 'candidate' };
                             this.isLoggedIn = true;
 
-                            // Resume where they left off if session exists in DB
-                            if (data.session) {
-                                const s = data.session;
+                            // Resume where they left off ΓÇö server session first, then local backup
+                            const serverSession = data.session;
+                            const localBackup = (() => {
+                                try { return JSON.parse(localStorage.getItem('vlab_session_' + data.user.email) || 'null'); } catch { return null; }
+                            })();
+                            const s = serverSession || localBackup;
+
+                            if (s) {
                                 this.currentView = s.currentView || 'dashboard';
                                 this.adminTab = s.adminTab || 'overview';
                                 if (s.selectedTopicId)
@@ -2670,19 +2900,24 @@
                                     const proj = this.projects.find(p => p.id === s.activeProjectId);
                                     if (proj) {
                                         this.activeProject = proj;
-                                        // Wait until DOM is ready, then load files smoothly
                                         setTimeout(() => { this.selectProject(proj); }, 50);
                                     } else this.activeProject = this.projects[0];
                                 } else {
                                     this.activeProject = this.projects[0];
                                 }
+                                // Clean up the backup now that we've restored
+                                localStorage.removeItem('vlab_session_' + data.user.email);
                             } else {
                                 this.currentView = 'dashboard';
+                                // Auto-select Visteon topic for Visteon users
+                                this.selectedTopic = this.topics.find(t => t.id === 'VISTEON_C') || this.topics[0];
                             }
 
                             // Save the active state locally so it's not lost on refresh
                             this.saveSession();
                             this._loadServerProgress();
+                            // Restore violations from localStorage (survives page close/refresh)
+                            this._restoreViolations(data.user.email);
                         }
                     } catch (e) {
                         alert('Network error. Make sure the server is running.');
@@ -2710,6 +2945,9 @@
                             this.adminTab = 'overview';
                             this.isLoggedIn = true;
                             this.saveSession();
+                            // Auto-load submissions and candidates for dashboard
+                            this.loadAllSubmissions();
+                            this.loadCandidates();
                         }
                     } catch (e) {
                         alert('Network error.');
@@ -2718,6 +2956,63 @@
                     }
                 },
 
+                // DB health check ΓÇö works on free tier (no shell needed)
+                async runDbDiagnostics() {
+                    if (this.dbDiagLoading) return;
+                    this.dbDiagLoading = true;
+                    this.dbDiagResult = null;
+                    try {
+                        const r = await fetch('/api/admin/db-stats', { headers: this._authHeaders() });
+                        if (!r.ok) throw new Error('Server returned ' + r.status);
+                        this.dbDiagResult = await r.json();
+                        this.showDbDiag = true;
+                        // Also sync into allSubmissions if DB has more than what's loaded
+                        if (this.dbDiagResult.submissions.total > this.allSubmissions.length) {
+                            this.loadAllSubmissions();
+                        }
+                    } catch(e) {
+                        alert('DB check failed: ' + e.message + '\n\nMake sure the latest server.js is deployed on Render.');
+                    } finally {
+                        this.dbDiagLoading = false;
+                    }
+                },
+
+                // Off-day submission check ΓÇö Mon(1), Wed(3), Fri(5) are allowed
+                checkOffDayThenSubmit() {
+                    const day = new Date().getDay(); // 0=Sun,1=Mon,...,6=Sat
+                    const isAllowedDay = [1, 3, 5].includes(day);
+                    if (!isAllowedDay) {
+                        this.showOffDayWarning = true;
+                    } else {
+                        this.showSubmitModal = true;
+                    }
+                },
+
+                // Manual weekly report trigger for admins
+                async sendWeeklyReport() {
+                    if (this.weeklyReportSending) return;
+                    this.weeklyReportSending = true;
+                    const weekLabel = `Week ending ${new Date().toLocaleDateString('en-IN', { day: '2-digit', month: 'short', year: 'numeric' })}`;
+                    try {
+                        const r = await fetch('/api/admin/send-weekly-report', {
+                            method: 'POST',
+                            headers: this._authHeaders(),
+                            body: JSON.stringify({ weekLabel })
+                        });
+                        const data = await r.json();
+                        if (data.success) {
+                            this.successToast = `≡ƒôº Weekly report sent to ${data.sentTo?.join(', ')}`;
+                        } else {
+                            alert('Failed to send report: ' + (data.error || 'Unknown error'));
+                        }
+                    } catch (e) {
+                        alert('Network error sending report.');
+                    } finally {
+                        this.weeklyReportSending = false;
+                        setTimeout(() => { this.successToast = ''; }, 5000);
+                    }
+                },
+
                 selectTopic(topic) {
                     this.selectedTopic = topic;
                     this.currentView = 'workspace';
@@ -2725,6 +3020,10 @@
                 },
 
                 async selectProject(project) {
+                    // Bank time from previous problem before switching
+                    if (this.problemStartTime && this.activeProject && this.activeProject.id !== project.id) {
+                        this.bankRemainingTime();
+                    }
                     this.activeProject = project;
                     this.activeFileIdx = 0;
                     this.wsTab = 'editor';
@@ -2758,6 +3057,10 @@
                         this.activeFiles = JSON.parse(JSON.stringify(project.files || []));
                     }
                     this.saveSession();
+                    // Start problem timer for candidates
+                    if (this.isLoggedIn && this.user.role === 'candidate') {
+                        this.startProblemTimer();
+                    }
                 },
 
                 get filteredProjects() {
@@ -2773,28 +3076,23 @@
                     const urlParams = new URLSearchParams(window.location.search);
                     const isVisteon = (typeof window.isVisteonUser === 'function' && window.isVisteonUser(this.user?.email)) || (urlParams.get('client') === 'visteon');
 
-                    // 1. Admin sees everything (unless explicitly filtered)
+                    // 1. Visteon users ALWAYS see ONLY Visteon topics (highest priority)
+                    if (isVisteon) {
+                        return this.topics.filter(t => t.id === 'VISTEON_C');
+                    }
+
+                    // 2. Admin sees everything
                     if (this.user && this.user.role === 'admin') {
                         let base = this.topicFilter.length ? this.topics.filter(t => this.topicFilter.includes(t.id)) : this.topics;
-                        // Always allow admin to see VISTEON_C even if filtered
-                        if (!base.some(t => t.id === 'VISTEON_C')) {
-                            const visteonTopic = this.topics.find(t => t.id === 'VISTEON_C');
-                            if (visteonTopic) base.push(visteonTopic);
-                        }
                         return base.filter(Boolean);
                     }
 
-                    // 2. Filter by topicVisibility from JSON (if available)
+                    // 3. Filter by topicVisibility from JSON (if available)
                     if (Object.keys(this.topicVisibility).length > 0) {
                         return this.topics.filter(t => this.topicVisibility[t.name] === true);
                     }
 
-                    // 3. Visteon users see ONLY VISTEON_C
-                    if (isVisteon) {
-                        return this.topics.filter(t => t.id === 'VISTEON_C');
-                    }
-
-                    // 4. Others see filtered topics (excluding VISTEON_C)
+                    // 4. Others see filtered topics (excluding Visteon topics)
                     let base = this.topicFilter.length ? this.topics.filter(t => this.topicFilter.includes(t.id)) : this.topics;
                     return base.filter(t => t.id !== 'VISTEON_C');
                 },
@@ -2803,31 +3101,42 @@
                     const urlParams = new URLSearchParams(window.location.search);
                     const isVisteon = (typeof window.isVisteonUser === 'function' && window.isVisteonUser(this.user?.email)) || (urlParams.get('client') === 'visteon');
 
-                    // 1. Admin sees everything (unless explicitly filtered)
+                    // 1. Visteon users ALWAYS see ONLY Visteon projects (highest priority)
+                    if (isVisteon) {
+                        return this.projects.filter(p => p.topicId === 'VISTEON_C');
+                    }
+
+                    // 2. Admin sees everything
                     if (this.user && this.user.role === 'admin') {
                         if (!this.topicFilter.length) return this.projects;
-                        return this.projects.filter(p => this.topicFilter.includes(p.topicId) || p.topicId === 'VISTEON_C');
+                        return this.projects.filter(p => this.topicFilter.includes(p.topicId));
                     }
 
-                    // 2. Filter by topicVisibility from JSON (if available)
+                    // 3. Filter by topicVisibility from JSON (if available)
                     if (Object.keys(this.topicVisibility).length > 0) {
                         const visibleTopicIds = this.topics.filter(t => this.topicVisibility[t.name] === true).map(t => t.id);
                         return this.projects.filter(p => visibleTopicIds.includes(p.topicId));
                     }
 
-                    // 3. Visteon users see ONLY VISTEON_C
-                    if (isVisteon) {
-                        return this.projects.filter(p => p.topicId === 'VISTEON_C');
-                    }
-
-                    // 4. Others see filtered projects (excluding VISTEON_C)
+                    // 4. Others see filtered projects (excluding Visteon topics)
                     let allowed = this.projects.filter(p => p.topicId !== 'VISTEON_C');
                     if (!this.topicFilter.length) return allowed;
                     return allowed.filter(p => this.topicFilter.includes(p.topicId));
                 },
 
+                get filteredSubmissions() {
+                    const q = (this.adminSearch || '').toLowerCase().trim();
+                    return this.allSubmissions.filter(s => {
+                        if (!q) return true;
+                        const name = (s.candidate_name || '').toLowerCase();
+                        const email = (s.email || '').toLowerCase();
+                        const title = (s.project_title || '').toLowerCase();
+                        return name.includes(q) || email.includes(q) || title.includes(q);
+                    });
+                },
+
                 get solvedCount() { return this.solvedProjectIds.length; },
-                get totalProgress() { return Math.round((this.solvedCount / (this.selectedTopic?.id === 'VISTEON_C' ? 21 : (this.allowedProjects.length || this.projects.length))) * 100) || 0; },
+                get totalProgress() { return Math.round((this.solvedCount / (this.selectedTopic?.id === 'VISTEON_C' ? 20 : (this.allowedProjects.length || this.projects.length))) * 100) || 0; },
                 get totalPointsEarned() {
                     return this.projects.filter(p => this.solvedProjectIds.includes(p.id))
                         .reduce((sum, p) => sum + p.points, 0);
@@ -2871,10 +3180,22 @@
                             cpp(),
                             oneDark,
                             EditorView.lineWrapping,
-                            EditorView.editable.of(true), // Explicitly make editable
+                            EditorView.editable.of(true),
                             EditorView.updateListener.of((update) => {
                                 if (update.docChanged && this.activeFiles[this.activeFileIdx]) {
                                     this.activeFiles[this.activeFileIdx].content = update.state.doc.toString();
+                                    // Debounced auto-save: 2s after user stops typing
+                                    clearTimeout(this._autoSaveTimer);
+                                    this._autoSaveTimer = setTimeout(() => {
+                                        if (this.isLoggedIn && this.activeProject) {
+                                            // Save to localStorage immediately
+                                            localStorage.setItem('code_' + this.activeProject.id, JSON.stringify(this.activeFiles));
+                                            // Save to server in background
+                                            const isSolved = this.solvedProjectIds.includes(this.activeProject.id);
+                                            this._saveProgressToServer(this.activeProject.id, this.activeFiles, isSolved);
+                                            this._showAutoSaveIndicator();
+                                        }
+                                    }, 2000);
                                 }
                             })
                         ],
@@ -3118,10 +3439,8 @@
                             // Save code to server + localStorage
                             localStorage.setItem('code_' + this.activeProject.id, JSON.stringify(this.activeFiles));
                             this._saveProgressToServer(this.activeProject.id, this.activeFiles, true);
-                            // Auto-download solution file
-                            this.downloadSolution();
                             // Show success toast
-                            this.successToast = `≡ƒÄë ${this.activeProject.title} ΓÇö Completed! Solution saved.`;
+                            this.successToast = `≡ƒÄë ${this.activeProject.title} ΓÇö Completed! Assignment submitted.`;
                             setTimeout(() => { this.successToast = ''; }, 5000);
                             // Run AI assessment automatically
                             this.runAIAssessment();
@@ -3161,20 +3480,6 @@
                     this.isAssessing = false;
                 },
 
-                downloadSolution() {
-                    const files = this.activeFiles;
-                    const projectId = this.activeProject.id;
-                    files.forEach(f => {
-                        const blob = new Blob([f.content], { type: 'text/plain' });
-                        const url = URL.createObjectURL(blob);
-                        const a = document.createElement('a');
-                        a.href = url;
-                        a.download = `${projectId}_${f.name}`;
-                        a.click();
-                        URL.revokeObjectURL(url);
-                    });
-                },
-
                 async compileJudge0(source_code, stdin) {
                     const API_URL = "https://judge0-ce.p.sira.sh";
 
@@ -3293,71 +3598,87 @@
                     }
                 },
 
+                async loadCandidates() {
+                    try {
+                        const r = await fetch('/api/users', { headers: this._authHeaders() });
+                        if (r.ok) {
+                            const data = await r.json();
+                            this.candidates = Array.isArray(data) ? data.filter(u => u.role === 'candidate') : [];
+                        }
+                    } catch(e) { console.error('Failed to load candidates:', e); }
+                },
+
                 async loadAllSubmissions() {
+                    this.submissionsLoading = true;
+                    this.submissionsError = '';
                     try {
                         let url = '/api/submissions?';
-                        if (this.adminDayFilter) url += 'day=' + this.adminDayFilter + '&';
-                        if (this.adminSubFilter && this.adminSubFilter !== 'all') url += 'status=' + this.adminSubFilter;
+                        if (this.adminSubFilter && this.adminSubFilter !== 'all') url += 'status=' + this.adminSubFilter + '&';
                         const response = await fetch(url, { headers: this._authHeaders() });
-                        this.allSubmissions = await response.json();
+                        if (!response.ok) {
+                            const err = await response.json().catch(() => ({}));
+                            throw new Error(err.error || 'Server returned ' + response.status);
+                        }
+                        const data = await response.json();
+                        this.allSubmissions = Array.isArray(data) ? data : [];
                     } catch (e) {
                         console.error('Failed to load submissions:', e);
+                        this.submissionsError = 'Failed to load submissions: ' + e.message + '. Click Retry to try again.';
                         this.allSubmissions = [];
+                    } finally {
+                        this.submissionsLoading = false;
                     }
                 },
 
-                async openGrading(sub) {
-                    this.gradingSubmission = sub;
-                    this.gradingDetail = null;
-                    this.gradeComments = '';
-                    // Pre-fill scores if already graded
-                    if (sub.grade && sub.grade.scores) {
-                        this.gradeScores = { ...sub.grade.scores };
-                        this.gradeComments = sub.grade.comments || '';
-                    } else {
-                        this.gradeScores = { correctness: 0, codeQuality: 0, logic: 0, style: 0 };
+                async loadTopicVisibility() {
+                    this.topicLoadError = false;
+                    try {
+                        const res = await fetch('/topics');
+                        if (!res.ok) throw new Error('Server error');
+                        this.topicVisibility = await res.json();
+                    } catch (e) {
+                        console.error('Failed to load topics:', e);
+                        this.topicLoadError = true;
                     }
-                    // Fetch full submission detail (with code)
+                },
+
+                async toggleTopic(name, newState) {
+                    this.topicSaving = name;
                     try {
-                        const response = await fetch('/api/submission/' + sub.id, {
-                            headers: this._authHeaders()
+                        const res = await fetch('/update-topic', {
+                            method: 'POST',
+                            headers: { 'Content-Type': 'application/json' },
+                            body: JSON.stringify({ topic: name, enabled: newState })
                         });
-                        this.gradingDetail = await response.json();
+                        if (!res.ok) throw new Error('Save failed');
+                        // Update local state immediately for instant UI feedback
+                        this.topicVisibility = { ...this.topicVisibility, [name]: newState };
+                        this.topicSaveMsg = `"${name}" ${newState ? 'enabled' : 'disabled'}`;
+                        setTimeout(() => { this.topicSaveMsg = ''; }, 2500);
                     } catch (e) {
-                        console.error('Failed to load submission detail:', e);
+                        console.error('Failed to update topic:', e);
+                        alert('Failed to save. Please try again.');
+                    } finally {
+                        this.topicSaving = null;
                     }
                 },
 
-                async submitGrade() {
-                    if (this.isGrading || !this.gradingSubmission) return;
-                    this.isGrading = true;
+                async openGrading(sub) {
+                    this.gradingSubmission = sub;
+                    this.gradingDetail = null;
+                    // Fetch full submission detail (with code + testResults)
                     try {
-                        const response = await fetch('/api/grade', {
-                            method: 'POST',
-                            headers: this._authHeaders(),
-                            body: JSON.stringify({
-                                submissionId: this.gradingSubmission.id,
-                                scores: this.gradeScores,
-                                comments: this.gradeComments,
-                                trainerName: this.user.name || 'Trainer'
-                            })
+                        const response = await fetch('/api/submission/' + sub.id, {
+                            headers: this._authHeaders()
                         });
-                        const result = await response.json();
-                        if (result.success) {
-                            this.successToast = 'Γ£à Grade submitted successfully! Composite: ' + result.grade.compositeScore + '%';
-                            setTimeout(() => { this.successToast = ''; }, 5000);
-                            this.gradingSubmission = null;
-                            this.gradingDetail = null;
-                            this.loadAllSubmissions();
-                        } else {
-                            this.successToast = 'Γ¥î Grading failed: ' + (result.error || 'Unknown error');
-                            setTimeout(() => { this.successToast = ''; }, 4000);
-                        }
+                        const detail = await response.json();
+                        // Merge full detail back into gradingSubmission for report panel
+                        this.gradingSubmission = { ...sub, ...detail };
+                        this.gradingDetail = detail;
                     } catch (e) {
-                        this.successToast = 'Γ¥î Could not reach server.';
-                        setTimeout(() => { this.successToast = ''; }, 4000);
+                        console.error('Failed to load submission detail:', e);
+                        this.gradingDetail = { code: [], testResults: [] };
                     }
-                    this.isGrading = false;
                 },
 
                 downloadLogs() {
@@ -3406,90 +3727,381 @@
                     window.URL.revokeObjectURL(url);
                 },
 
-                downloadAllReports() {
-                    if (!this.allSubmissions || !this.allSubmissions.length) {
-                        alert("No submissions to export.");
-                        return;
-                    }
-                    const data = this.allSubmissions.map(s => {
-                        return {
-                            "Submission ID": s.id || '-',
-                            "Candidate Name": s.candidateName || s.candidate_name || '-',
-                            "Email": s.email || '-',
-                            "Project ID": s.projectId || s.project_id || '-',
-                            "Project Title": s.projectTitle || s.project_title || '-',
-                            "Topic": s.topicName || s.topic_name || '-',
-                            "Day": s.day || '-',
-                            "Status": s.status || '-',
-                            "Auto Score (%)": s.autoScore || s.auto_score || 0,
-                            "Composite Score (%)": s.grade && s.grade.compositeScore ? s.grade.compositeScore : 0,
-                            "Violations": s.violationCount || s.violation_count || 0,
-                            "Date Submitted": s.submittedAt ? new Date(s.submittedAt).toLocaleString() : '-'
-                        };
-                    });
-                    
-                    const ws = XLSX.utils.json_to_sheet(data);
-                    const wb = XLSX.utils.book_new();
-                    XLSX.utils.book_append_sheet(wb, ws, "Submissions");
-                    XLSX.writeFile(wb, "Visteon_Embedded_Lab_Report.xlsx");
-                },
-
-                sendWeeklyReport() {
-                    this.weeklyReportSending = true;
-                    setTimeout(() => {
-                        this.weeklyReportSending = false;
-                        this.successToast = 'Weekly report dispatched to management successfully!';
-                        setTimeout(() => { this.successToast = '' }, 3000);
-                    }, 1500);
-                },
-
-                generateReport(candidate) {
-                    const reportContent = `Evaluation Dossier - ${candidate.name}
---------------------------------------------------
-Email: ${candidate.email}
-Completed Assignments: ${candidate.completed || 0}
-Hours Logged: ${candidate.hours || 0}
-Progress: ${candidate.progress || 0}%
-
-Strong Areas: ${candidate.strong && candidate.strong.length ? candidate.strong.join(', ') : 'None identified'}
-Weak Areas: ${candidate.weak && candidate.weak.length ? candidate.weak.join(', ') : 'None identified'}
-
-Generated from SkillLync VirtualLab System.`;
-
-                    const blob = new Blob([reportContent], { type: 'text/plain' });
-                    const url = window.URL.createObjectURL(blob);
-                    const a = document.createElement('a');
-                    a.href = url;
-                    a.download = `Dossier_${candidate.name.replace(/\s+/g, '_')}.txt`;
-                    document.body.appendChild(a);
-                    a.click();
-                    document.body.removeChild(a);
-                    window.URL.revokeObjectURL(url);
-                    
-                    this.successToast = `Dossier for ${candidate.name} downloaded successfully!`;
-                    setTimeout(() => { this.successToast = '' }, 3000);
-                },
-
                 // ΓÇöΓÇöΓÇö Anti-Cheat / Proctoring ΓÇöΓÇöΓÇö
 
 
                 registerViolation(reason) {
                     if (!this.isLoggedIn || this.user.role === 'admin') return;
                     this.violationCount++;
-                    this.violationLog.push({
+                    const entry = {
                         time: new Date().toLocaleTimeString(),
                         reason: reason,
-                        count: this.violationCount
-                    });
-                    // Show critical warning at 10+ violations
+                        count: this.violationCount,
+                        problem: this.activeProject?.title || 'ΓÇö'
+                    };
+                    this.violationLog.push(entry);
+                    // Persist violations to localStorage so they survive page refresh
+                    try {
+                        localStorage.setItem('vlab_violations_' + this.user.email,
+                            JSON.stringify({ count: this.violationCount, log: this.violationLog }));
+                    } catch(e) {}
                     if (this.violationCount >= 10) {
                         this.criticalViolationWarning = true;
                     } else {
                         this.violationWarning = true;
-                        // auto-dismiss after 3 seconds
                         setTimeout(() => { this.violationWarning = false; }, 3000);
                     }
-                }
+                },
+
+                _showAutoSaveIndicator() {
+                    this.autoSaveStatus = 'saved';
+                    clearTimeout(this.autoSaveTimer);
+                    this.autoSaveTimer = setTimeout(() => { this.autoSaveStatus = ''; }, 3000);
+                },
+
+                // ΓöÇΓöÇ Problem Timer Pool ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
+                startProblemTimer() {
+                    this.stopProblemTimer();
+                    this.problemStartTime = Date.now();
+                    this.problemElapsed = 0;
+                    this.problemTimeWarning = false;
+                    this.problemTimerExpired = false;
+
+                    this.problemTimerInterval = setInterval(() => {
+                        const elapsed = Math.floor((Date.now() - this.problemStartTime) / 1000);
+                        this.problemElapsed = elapsed;
+                        const budget = this.PROBLEM_LIMIT_SECS;
+                        const remaining = budget - elapsed;
+
+                        // 1 minute warning
+                        if (remaining === 60) {
+                            this.problemTimeWarning = true;
+                            setTimeout(() => { this.problemTimeWarning = false; }, 8000);
+                        }
+                        // Time expired
+                        if (remaining <= 0) {
+                            this.problemTimerExpired = true;
+                            this.stopProblemTimer();
+                        }
+                    }, 1000);
+                },
+
+                stopProblemTimer() {
+                    if (this.problemTimerInterval) {
+                        clearInterval(this.problemTimerInterval);
+                        this.problemTimerInterval = null;
+                    }
+                },
+
+                bankRemainingTime() {
+                    // Log time spent on this problem and stop the timer
+                    const elapsed = Math.floor((Date.now() - this.problemStartTime) / 1000);
+                    if (this.activeProject) {
+                        this.problemTimeLog[this.activeProject.id] = elapsed;
+                        this.totalTimeUsed += elapsed;
+                    }
+                    this.stopProblemTimer();
+                    this.problemTimerExpired = false;
+                },
+
+                get timerRemainingSeconds() {
+                    const budget = this.PROBLEM_LIMIT_SECS;
+                    return Math.max(0, budget - this.problemElapsed);
+                },
+
+                get timerDisplay() {
+                    const s = this.timerRemainingSeconds;
+                    const m = Math.floor(s / 60);
+                    const sec = s % 60;
+                    return String(m).padStart(2, '0') + ':' + String(sec).padStart(2,'0');
+                },
+
+                get timerColor() {
+                    const s = this.timerRemainingSeconds;
+                    if (s <= 60) return 'text-red-400';
+                    if (s <= 180) return 'text-amber-400';
+                    return 'text-emerald-400';
+                },
+
+                moveToNextProblem() {
+                    this.bankRemainingTime();
+                    this.problemTimerExpired = false;
+                    // Find next project in allowed list
+                    const list = this.allowedProjects;
+                    const idx = list.findIndex(p => p.id === this.activeProject?.id);
+                    const next = list[idx + 1];
+                    if (next) {
+                        this.selectProject(next);
+                    } else {
+                        this.currentView = 'dashboard';
+                    }
+                },
+
+                // ΓöÇΓöÇ Excel Report Generator ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
+                _field(s, camel, snake) {
+                    return s[camel] !== undefined ? s[camel] : s[snake];
+                },
+
+                _buildExcelReport(submissions, title) {
+                    if (typeof XLSX === 'undefined') {
+                        alert('Excel library not loaded. Please refresh the page.');
+                        return;
+                    }
+                    const wb = XLSX.utils.book_new();
+                    const f = (s, c, k) => this._field(s, c, k);
+
+                    // ΓöÇΓöÇ Collect candidate list ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
+                    const candidateMap = {};
+                    submissions.forEach(s => {
+                        const email = s.email;
+                        const name = f(s,'candidateName','candidate_name') || email;
+                        if (!candidateMap[email]) candidateMap[email] = { name, email, subs: [] };
+                        candidateMap[email].subs.push(s);
+                    });
+                    const candidates = Object.values(candidateMap);
+
+                    // ΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉ
+                    // SHEET 1: Candidate Summary
+                    // ΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉ
+                    const summaryRows = [[
+                        'Rank', 'Candidate Name', 'Email',
+                        'Problems Submitted', 'Total Tests Passed', 'Total Tests',
+                        'Pass Rate %', 'Avg Score %',
+                        'Total Violations', 'Tab Switches', 'Copy/Paste Attempts',
+                        'Status'
+                    ]];
+
+                    const ranked = candidates.map(c => {
+                        const subs = c.subs;
+                        const totalTests = subs.reduce((a, s) => a + (f(s,'testsTotal','tests_total') || 0), 0);
+                        const passedTests = subs.reduce((a, s) => a + (f(s,'testsPassed','tests_passed') || 0), 0);
+                        const avgScore = subs.length ? Math.round(subs.reduce((a, s) => a + (f(s,'autoScore','auto_score') || 0), 0) / subs.length) : 0;
+                        const totalViolations = subs.reduce((a, s) => a + (f(s,'violationCount','violation_count') || 0), 0);
+
+                        let tabSwitches = 0, copyPaste = 0;
+                        subs.forEach(s => {
+                            const vlog = this._parseViolationLog(f(s,'violationLog','violation_log'));
+                            tabSwitches += vlog.filter(v => v.reason && (v.reason.includes('Tab switch') || v.reason.includes('focus lost'))).length;
+                            copyPaste += vlog.filter(v => v.reason && (v.reason.includes('Copy') || v.reason.includes('Paste') || v.reason.includes('Cut'))).length;
+                        });
+
+                        const passRate = totalTests > 0 ? Math.round((passedTests / totalTests) * 100) : 0;
+                        return { c, subs, totalTests, passedTests, avgScore, totalViolations, tabSwitches, copyPaste, passRate };
+                    }).sort((a, b) => b.avgScore - a.avgScore);
+
+                    ranked.forEach((r, i) => {
+                        summaryRows.push([
+                            i + 1,
+                            r.c.name,
+                            r.c.email,
+                            r.subs.length,
+                            r.passedTests,
+                            r.totalTests,
+                            r.passRate + '%',
+                            r.avgScore + '%',
+                            r.totalViolations,
+                            r.tabSwitches,
+                            r.copyPaste,
+                            r.totalViolations >= 10 ? '≡ƒÜ¿ High Risk' : r.totalViolations >= 3 ? 'ΓÜá∩╕Å Warning' : 'Γ£à Clean'
+                        ]);
+                    });
+
+                    const ws1 = XLSX.utils.aoa_to_sheet(summaryRows);
+                    ws1['!cols'] = [
+                        { wch: 6 }, { wch: 25 }, { wch: 35 }, { wch: 18 }, { wch: 16 },
+                        { wch: 14 }, { wch: 12 }, { wch: 12 }, { wch: 16 }, { wch: 14 },
+                        { wch: 20 }, { wch: 14 }
+                    ];
+                    XLSX.utils.book_append_sheet(wb, ws1, 'Candidate Summary');
+
+                    // ΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉ
+                    // SHEET 2: Problem-wise Breakdown
+                    // ΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉ
+                    const probRows = [[
+                        'Candidate Name', 'Email', 'Problem ID', 'Problem Title',
+                        'Score %', 'Tests Passed', 'Tests Total', 'Pass Rate %',
+                        'Violations on Problem', 'Submitted At', 'Status'
+                    ]];
+
+                    submissions.forEach(s => {
+                        const vlog = this._parseViolationLog(f(s,'violationLog','violation_log'));
+                        const title_ = f(s,'projectTitle','project_title') || f(s,'projectId','project_id');
+                        const probViolations = vlog.filter(v => v.problem === title_).length;
+                        const totalTests = f(s,'testsTotal','tests_total') || 0;
+                        const passedTests = f(s,'testsPassed','tests_passed') || 0;
+                        const passRate = totalTests > 0 ? Math.round((passedTests / totalTests) * 100) : 0;
+                        const submittedAt = f(s,'submittedAt','submitted_at');
+                        probRows.push([
+                            f(s,'candidateName','candidate_name') || s.email,
+                            s.email,
+                            f(s,'projectId','project_id') || 'ΓÇö',
+                            title_ || 'ΓÇö',
+                            f(s,'autoScore','auto_score') || 0,
+                            passedTests,
+                            totalTests,
+                            passRate + '%',
+                            probViolations,
+                            submittedAt ? new Date(submittedAt).toLocaleString() : 'ΓÇö',
+                            s.status || 'ΓÇö'
+                        ]);
+                    });
+
+                    const ws2 = XLSX.utils.aoa_to_sheet(probRows);
+                    ws2['!cols'] = [
+                        { wch: 25 }, { wch: 35 }, { wch: 16 }, { wch: 40 },
+                        { wch: 10 }, { wch: 14 }, { wch: 12 }, { wch: 12 },
+                        { wch: 20 }, { wch: 22 }, { wch: 12 }
+                    ];
+                    XLSX.utils.book_append_sheet(wb, ws2, 'Problem Breakdown');
+
+                    // ΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉ
+                    // SHEET 3: Test Case Details
+                    // ΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉ
+                    const testRows = [[
+                        'Candidate Name', 'Email', 'Problem Title', 'Test Case Name', 'Result', 'Expected Output', 'Actual Output'
+                    ]];
+
+                    submissions.forEach(s => {
+                        const tests = this._parseJson(f(s,'testResults','test_results'));
+                        const title_ = f(s,'projectTitle','project_title') || f(s,'projectId','project_id');
+                        if (!tests || !tests.length) {
+                            testRows.push([
+                                f(s,'candidateName','candidate_name') || s.email, s.email,
+                                title_, 'ΓÇö', 'No test data', 'ΓÇö', 'ΓÇö'
+                            ]);
+                            return;
+                        }
+                        tests.forEach(t => {
+                            testRows.push([
+                                f(s,'candidateName','candidate_name') || s.email,
+                                s.email,
+                                title_,
+                                t.name || 'ΓÇö',
+                                t.passed ? 'PASS' : 'FAIL',
+                                t.expectedOutput || t.expected || 'ΓÇö',
+                                t.actualOutput || t.output || t.actual || 'ΓÇö'
+                            ]);
+                        });
+                    });
+
+                    const ws3 = XLSX.utils.aoa_to_sheet(testRows);
+                    ws3['!cols'] = [
+                        { wch: 25 }, { wch: 35 }, { wch: 40 }, { wch: 30 },
+                        { wch: 10 }, { wch: 30 }, { wch: 30 }
+                    ];
+                    XLSX.utils.book_append_sheet(wb, ws3, 'Test Case Details');
+
+                    // ΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉ
+                    // SHEET 4: Violations Log
+                    // ΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉ
+                    const violRows = [[
+                        'Candidate Name', 'Email', 'Violation #', 'Timestamp',
+                        'Violation Type', 'Problem / Context', 'Severity'
+                    ]];
+
+                    let hasViolations = false;
+                    submissions.forEach(s => {
+                        const vlog = this._parseViolationLog(f(s,'violationLog','violation_log'));
+                        if (!vlog.length) return;
+                        hasViolations = true;
+                        vlog.forEach((v, i) => {
+                            const reason = v.reason || 'ΓÇö';
+                            let severity = 'Low';
+                            if (reason.includes('Tab switch') || reason.includes('focus lost')) severity = 'Medium';
+                            if (reason.includes('Copy') || reason.includes('Paste') || reason.includes('Cut')) severity = 'High';
+                            if (reason.includes('DevTools')) severity = 'Critical';
+                            violRows.push([
+                                f(s,'candidateName','candidate_name') || s.email,
+                                s.email,
+                                i + 1,
+                                v.time || 'ΓÇö',
+                                reason,
+                                v.problem || f(s,'projectTitle','project_title') || 'ΓÇö',
+                                severity
+                            ]);
+                        });
+                    });
+
+                    if (!hasViolations) {
+                        violRows.push(['No violations recorded', '', '', '', '', '', '']);
+                    }
+
+                    const ws4 = XLSX.utils.aoa_to_sheet(violRows);
+                    ws4['!cols'] = [
+                        { wch: 25 }, { wch: 35 }, { wch: 12 }, { wch: 20 },
+                        { wch: 40 }, { wch: 35 }, { wch: 12 }
+                    ];
+                    XLSX.utils.book_append_sheet(wb, ws4, 'Violations Log');
+
+                    // ΓöÇΓöÇ Save file ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
+                    const date = new Date().toISOString().slice(0, 10);
+                    XLSX.writeFile(wb, `${title}_${date}.xlsx`);
+                },
+
+                _parseViolationLog(raw) {
+                    try {
+                        const parsed = typeof raw === 'string' ? JSON.parse(raw || '[]') : (raw || []);
+                        return Array.isArray(parsed) ? parsed : [];
+                    } catch { return []; }
+                },
+
+                _parseJson(raw) {
+                    try {
+                        const parsed = typeof raw === 'string' ? JSON.parse(raw || '[]') : (raw || []);
+                        return Array.isArray(parsed) ? parsed : [];
+                    } catch { return []; }
+                },
+
+                async downloadCandidateReport(candidate) {
+                    // Fetch full submissions with violation_log for this candidate
+                    let subs = this.allSubmissions.filter(s => s.email === candidate.email);
+                    if (!subs.length) { alert('No submissions found for this candidate.'); return; }
+                    // Enrich each sub with violation_log from full detail endpoint
+                    subs = await Promise.all(subs.map(async s => {
+                        if (s.violationLog || s.violation_log) return s;
+                        try {
+                            const r = await fetch('/api/submission/' + s.id, { headers: this._authHeaders() });
+                            const detail = await r.json();
+                            return { ...s, violation_log: detail.violation_log, test_results: detail.test_results };
+                        } catch { return s; }
+                    }));
+                    this._buildExcelReport(subs, `report_${candidate.email.replace('@', '_')}`);
+                },
+
+                async downloadAllReports() {
+                    if (!this.allSubmissions.length) { alert('No submissions to export.'); return; }
+                    this.successToast = 'ΓÅ│ Building Excel reportΓÇª';
+                    // Enrich all submissions with full violation_log
+                    const enriched = await Promise.all(this.allSubmissions.map(async s => {
+                        if (s.violationLog || s.violation_log) return s;
+                        try {
+                            const r = await fetch('/api/submission/' + s.id, { headers: this._authHeaders() });
+                            const detail = await r.json();
+                            return { ...s, violation_log: detail.violation_log, test_results: detail.test_results };
+                        } catch { return s; }
+                    }));
+                    this.successToast = '';
+                    this._buildExcelReport(enriched, 'VisteonVirtualLab_AllCandidates');
+                },
+
+                buildCandidateReport(candidate) {
+                    return `Report for ${candidate.name} (${candidate.email})`;
+                },
+
+                _parseViolationLog(raw) {
+                    try {
+                        const parsed = typeof raw === 'string' ? JSON.parse(raw || '[]') : (raw || []);
+                        return Array.isArray(parsed) ? parsed : [];
+                    } catch { return []; }
+                },
+
+                _parseJson(raw) {
+                    try {
+                        const parsed = typeof raw === 'string' ? JSON.parse(raw || '[]') : (raw || []);
+                        return Array.isArray(parsed) ? parsed : [];
+                    } catch { return []; }
+                },
+
             }
         }
     </script>
